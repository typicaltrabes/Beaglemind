---
phase: 10-mobile-pwa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/(dashboard)/dashboard-shell.tsx
  - apps/web/components/sidebar/sidebar.tsx
  - apps/web/lib/stores/ui-store.ts
  - apps/web/components/transcript/composer.tsx
  - apps/web/components/process-drawer-mobile.tsx
autonomous: true
requirements: [MOBI-01]
must_haves:
  truths:
    - "On viewport <768px, sidebar is hidden and a hamburger button appears in the header"
    - "Tapping hamburger opens sidebar as a full-screen overlay with backdrop"
    - "Transcript area spans full width on mobile"
    - "Composer sticks to bottom of viewport on mobile"
    - "Process drawer opens as full-screen overlay on mobile (not inline)"
  artifacts:
    - path: "apps/web/app/(dashboard)/dashboard-shell.tsx"
      provides: "Responsive shell with hamburger toggle and mobile sidebar overlay"
    - path: "apps/web/components/sidebar/sidebar.tsx"
      provides: "Mobile overlay mode with backdrop and close-on-navigate"
    - path: "apps/web/components/transcript/composer.tsx"
      provides: "Sticky bottom composer on mobile"
    - path: "apps/web/components/process-drawer-mobile.tsx"
      provides: "Full-screen overlay wrapper for process drawer on mobile"
  key_links:
    - from: "dashboard-shell.tsx"
      to: "sidebar.tsx"
      via: "hamburger button toggles ui-store sidebarOpen"
      pattern: "toggleSidebar"
    - from: "dashboard-shell.tsx"
      to: "ui-store.ts"
      via: "hamburger reads sidebarOpen state"
      pattern: "useUIStore"
---

<objective>
Make the entire dashboard layout responsive for mobile browsers.

Purpose: MOBI-01 requires the console to be usable on mobile. The sidebar must collapse to a hamburger menu, the transcript must go full-width, and the composer must stick to the bottom of the viewport. Per D-05, the process drawer is hidden on mobile but available as a full-screen overlay.

Output: Responsive dashboard that works on phones (< 768px viewport).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-mobile-pwa/10-CONTEXT.md

<interfaces>
<!-- Existing code the executor needs -->

From apps/web/lib/stores/ui-store.ts:
```typescript
interface UIState {
  activeProjectId: string | null;
  activeRunId: string | null;
  sidebarOpen: boolean;
}
interface UIActions {
  setActiveProject: (id: string | null) => void;
  setActiveRun: (id: string | null) => void;
  toggleSidebar: () => void;
}
export const useUIStore = create<UIState & UIActions>();
```

From apps/web/app/(dashboard)/dashboard-shell.tsx:
```typescript
export function DashboardShell({ children }: { children: React.ReactNode })
// Currently: flex layout with <Sidebar /> + main content column
// Header has: Beagle Agent Console title, ModeToggle, LogoutButton
```

From apps/web/components/sidebar/sidebar.tsx:
```typescript
export function Sidebar()
// Currently: `if (!sidebarOpen) return null;`
// Fixed w-[260px] aside with border-r
```

From apps/web/components/transcript/composer.tsx:
```typescript
export function Composer({ runId }: ComposerProps)
// Currently: border-t div with textarea + buttons, no sticky positioning
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make dashboard shell and sidebar responsive with hamburger menu</name>
  <files>
    apps/web/app/(dashboard)/dashboard-shell.tsx
    apps/web/components/sidebar/sidebar.tsx
    apps/web/lib/stores/ui-store.ts
  </files>
  <action>
    read_first:
    - apps/web/app/(dashboard)/dashboard-shell.tsx
    - apps/web/components/sidebar/sidebar.tsx
    - apps/web/lib/stores/ui-store.ts
    - apps/web/components/sidebar/project-list.tsx (understand sidebar children)

    Per D-04: Tailwind responsive breakpoints. Mobile = sidebar collapses to hamburger.

    1. **ui-store.ts**: Add `isMobile` derived state and `setSidebarOpen(open: boolean)` action (in addition to existing toggleSidebar). On mobile, sidebarOpen defaults to false. Keep desktop default as true.

    2. **dashboard-shell.tsx**: 
       - Add a hamburger button (Menu icon from lucide-react) in the header that is visible only on `md:hidden`. 
       - On desktop (md+), keep current layout unchanged.
       - Move the title to `flex-1` so hamburger sits left, title center, controls right on mobile.
       - Header should be `sticky top-0 z-30` so it's always visible on mobile scroll.

    3. **sidebar.tsx**: Two rendering modes based on viewport:
       - **Desktop (md+)**: Current behavior -- static aside, w-[260px], shows when sidebarOpen=true.
       - **Mobile (<md)**: Render as a fixed full-screen overlay (fixed inset-0 z-40) with a dark backdrop (bg-black/50). The sidebar panel slides from the left (w-[280px]). Tapping the backdrop or navigating to a link closes it (setSidebarOpen(false)).
       - Use `useEffect` with `matchMedia('(min-width: 768px)')` to detect viewport. On resize to desktop, ensure sidebar reverts to default open. On resize to mobile, close sidebar.
       - Add close (X) button at top of mobile sidebar overlay.
       - Wire Link clicks inside sidebar to close on mobile (usePathname + useEffect to close sidebar on route change).

    Do NOT use a CSS-only approach for the overlay -- the backdrop click handler needs JS. Use Tailwind classes for responsive hiding/showing of the hamburger button itself.
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && pnpm --filter @beagle-console/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    acceptance_criteria:
    - Hamburger icon visible in header on viewports < 768px, hidden on md+
    - Tapping hamburger opens sidebar as overlay with backdrop on mobile
    - Tapping backdrop or a nav link closes the sidebar overlay
    - On desktop (md+), sidebar behaves exactly as before (static, toggle via existing UI)
    - No layout shift or broken styles on desktop
  </done>
</task>

<task type="auto">
  <name>Task 2: Make composer sticky and process drawer mobile-aware</name>
  <files>
    apps/web/components/transcript/composer.tsx
    apps/web/components/process-drawer-mobile.tsx
  </files>
  <action>
    read_first:
    - apps/web/components/transcript/composer.tsx
    - apps/web/components/process-drawer.tsx (if exists -- the Studio mode drawer)
    - apps/web/app/(dashboard)/projects/[projectId]/runs/[runId]/page.tsx

    Per D-04: Composer sticks to bottom on mobile.
    Per D-05: Process drawer hidden on mobile, opens as full-screen overlay.

    1. **composer.tsx**: 
       - Add `sticky bottom-0 z-20` classes to the outer wrapper div. This ensures the composer stays pinned at the bottom of the run page scroll area on mobile.
       - On mobile, add `pb-safe` (or `pb-[env(safe-area-inset-bottom)]`) to account for iOS safe area / home indicator.
       - Ensure the Studio toolbar (verbosity, @mention) is still usable on mobile -- stack vertically if needed with `flex-wrap` on the toolbar div.

    2. **process-drawer-mobile.tsx**: Create a new wrapper component that:
       - On desktop (md+): renders children inline (passthrough, no wrapper).
       - On mobile (<md): renders children inside a fixed full-screen overlay (similar to sidebar mobile pattern) with a close button at top-right.
       - Export `MobileDrawerWrapper` that the run page can wrap around the existing ProcessDrawer.
       - Uses the same `matchMedia` pattern from Task 1.
       - The run page should conditionally wrap ProcessDrawer with this on mobile.

    3. Update the run page to wrap ProcessDrawer with MobileDrawerWrapper and add a "Process" floating button visible only on mobile in Studio mode that toggles the drawer overlay.

    Do NOT restructure the desktop layout. The mobile-only changes should be additive via responsive Tailwind classes and the wrapper component.
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && pnpm --filter @beagle-console/web exec tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>
    acceptance_criteria:
    - Composer is sticky at bottom of viewport on mobile
    - iOS safe area inset is accounted for
    - Process drawer is not visible inline on mobile
    - A "Process" button appears on mobile in Studio mode; tapping opens drawer as full-screen overlay
    - Desktop layout is completely unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Purely client-side layout changes, no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | I (Info Disclosure) | Mobile overlay sidebar | accept | Sidebar shows same data as desktop; no new exposure. Auth still required. |
</threat_model>

<verification>
- Open the app in Chrome DevTools responsive mode (iPhone 14 viewport)
- Verify hamburger appears, sidebar opens as overlay, closes on backdrop tap
- Navigate to a run page, verify composer sticks to bottom
- Toggle Studio mode, verify process drawer opens as overlay on mobile
- Switch to desktop viewport, verify no regressions
</verification>

<success_criteria>
- Console is fully navigable on a 390px-wide mobile viewport
- No horizontal scroll, no overlapping elements
- Sidebar, composer, and process drawer all adapt correctly
</success_criteria>

<output>
After completion, create `.planning/phases/10-mobile-pwa/10-01-SUMMARY.md`
</output>
