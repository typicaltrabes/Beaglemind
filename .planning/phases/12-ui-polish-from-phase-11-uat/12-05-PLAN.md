---
phase: 12-ui-polish-from-phase-11-uat
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - UAT-12-01
  - UAT-12-02
  - UAT-12-03
  - UAT-12-04
must_haves:
  truths:
    - "All four defects (UAT-12-01..04) are fixed in the deployed build at console.beaglemind.ai"
    - "Lucas re-runs the UAT pass and confirms each fix against the original 2026-04-27 screenshots"
    - "REQUIREMENTS.md UAT-12-01..04 statuses are updated from 'Pending' to 'Complete'"
    - "Deploy uses the existing manual workflow (push to GitHub, SSH beaglehq, git pull, docker build, docker compose up -d --force-recreate console-web)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability table marking UAT-12-01..04 Complete"
      contains: "UAT-12-01"
  key_links:
    - from: "console.beaglemind.ai/runs"
      to: "deployed apps/web/app/globals.css :root dark palette"
      via: "first-paint visual verification in fresh incognito session"
      pattern: "dark background"
    - from: "console.beaglemind.ai/projects/.../runs/..."
      to: "deployed h1 + WritersRoomSkeleton + chip-styled AgentMessage"
      via: "open a fresh run + an existing run, verify each defect"
      pattern: "visual verification"
---

<objective>
Deploy Plans 01–04 to `console.beaglemind.ai` and run the human UAT checkpoint that closes UAT-12-01..04. After Lucas confirms each defect is fixed against the original 2026-04-27 screenshots, update `.planning/REQUIREMENTS.md` to mark the four UAT items Complete.

Purpose: Phase 11 ended with a UAT pass that surfaced these four defects; Phase 12 only ships when the same UAT screenshots come back clean. CONTEXT.md `<decisions>` §Cross-cutting requires "Single deploy at end of phase using the existing manual workflow."

Output:
- Production build deployed via `git push` → SSH BeagleHQ → `git pull` → `docker build` → `docker compose up -d --force-recreate console-web`.
- Lucas re-screenshots `/runs`, a fresh run with no events (Writers' Room loading state), an active run with multiple agents (speaker chips + Herman capitalization), and a run page header (h1 title visible).
- REQUIREMENTS.md traceability table updated: UAT-12-01..04 → Complete.

This plan is in Wave 3 because it depends on all four implementation plans landing first. It is `autonomous: false` because Task 2 is a `checkpoint:human-verify` requiring Lucas to compare deployed UI against the original screenshots.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-ui-polish-from-phase-11-uat/12-CONTEXT.md
@.planning/phases/12-ui-polish-from-phase-11-uat/12-01-PLAN.md
@.planning/phases/12-ui-polish-from-phase-11-uat/12-02-PLAN.md
@.planning/phases/12-ui-polish-from-phase-11-uat/12-03-PLAN.md
@.planning/phases/12-ui-polish-from-phase-11-uat/12-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-deploy build verification + push to main + deploy on BeagleHQ</name>
  <files>
    (no source file changes; this task pushes already-committed work and runs deploy commands)
  </files>
  <read_first>
    .planning/phases/12-ui-polish-from-phase-11-uat/12-CONTEXT.md
  </read_first>
  <action>
    Run the deploy chain. CONTEXT.md `<decisions>` §Cross-cutting specifies the workflow exactly: "push to GitHub → SSH `beaglehq` → `git pull` in `/tmp/beagle-build` → `docker build` → `docker compose up -d --force-recreate console-web`."

    A) Pre-deploy build sanity (run from project root on the dev machine):
       ```bash
       cd apps/web && pnpm exec tsc --noEmit
       cd apps/web && pnpm exec vitest run
       ```
       Both commands must exit 0. If either fails, STOP and surface the failure to the user — do NOT push.

    B) Verify git is clean and on main:
       ```bash
       git status
       git rev-parse --abbrev-ref HEAD
       ```
       Expected: working tree clean, branch == `main` (or whatever the deploy branch is — check repository convention from Phase 11 SUMMARY if uncertain). All Plan 01–04 commits should already be in the log per the per-defect atomic-commit rule from CONTEXT.md.

    C) Push to GitHub:
       ```bash
       git push origin main
       ```

    D) SSH to BeagleHQ and deploy. Run these as a single ssh invocation for atomicity:
       ```bash
       ssh beaglehq 'cd /tmp/beagle-build && git pull && docker build -f apps/web/Dockerfile -t beagle-console-web:latest . && docker compose up -d --force-recreate console-web'
       ```
       (Adjust the docker-compose service name if the repo uses something other than `console-web`; verify against the Phase 1 / Phase 11 deploy commands documented in their SUMMARYs. CONTEXT.md uses `console-web` as the canonical service name.)

    E) Wait ~30s for the container to come up, then check:
       ```bash
       ssh beaglehq 'docker ps --filter name=console-web --format "table {{.Names}}\t{{.Status}}\t{{.Ports}}"'
       curl -s -o /dev/null -w "%{http_code}" https://console.beaglemind.ai/
       ```
       Expected: container status `Up`, HTTP 200 (or 307 redirect to /login — both indicate the app booted).

    F) Confirm static asset hash changed (smoke test that the new build is actually serving):
       ```bash
       curl -s https://console.beaglemind.ai/ | grep -oE '_next/static/[a-zA-Z0-9_-]+/_buildManifest' | head -1
       ```
       The build hash should differ from the pre-deploy version. (Optional sanity check; if the URL structure has changed since Phase 1, skip.)

    DO NOT:
    - Run the deploy if the local pre-deploy build (step A) fails.
    - Push to main if there are uncommitted changes (`git status` not clean).
    - Modify any source files in this task — all source changes belong to Plans 01–04.
    - Trigger a database migration (none of the four defects requires one — Plan 04's new endpoint reads existing columns).
  </action>
  <verify>
    <automated>ssh beaglehq 'docker ps --filter name=console-web --format "{{.Status}}"' | grep -q "^Up"</automated>
  </verify>
  <acceptance_criteria>
    - `cd apps/web && pnpm exec tsc --noEmit` exits 0 BEFORE push (executor runs and confirms)
    - `cd apps/web && pnpm exec vitest run` exits 0 BEFORE push (executor runs and confirms)
    - `git status` reports a clean working tree on the deploy branch BEFORE push
    - `git push origin main` succeeds (or whatever branch convention the repo uses)
    - SSH to BeagleHQ successful (`ssh beaglehq` without password prompt)
    - `git pull` on the server reports the new commits arriving
    - `docker build` exits 0 on the server
    - `docker compose up -d --force-recreate console-web` exits 0
    - `docker ps --filter name=console-web` shows `Status: Up` within 60 seconds
    - `curl -s -o /dev/null -w "%{http_code}" https://console.beaglemind.ai/` returns 200 or 3xx (server responsive)
  </acceptance_criteria>
  <done>
    The new build is live at https://console.beaglemind.ai/ — container is Up, HTTPS endpoint serves a 200/3xx, all four implementation plans are deployed atomically.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Lucas re-runs UAT against console.beaglemind.ai and confirms each defect is fixed</name>
  <what-built>
    All four UAT defects from the 2026-04-27 pass have shipped to production:
    - UAT-12-01: Run History page now uses the dark palette unconditionally (`:root` matches `.dark` in `globals.css`).
    - UAT-12-02: Writers' Room empty state now renders the `WritersRoomSkeleton` (Mo, Jarvis, Herman placeholders with avatars + "thinking…" + pulsing skeleton bars + "Mo, Jarvis, and Herman are getting ready…" subtitle).
    - UAT-12-03: AgentMessage speaker block is now a chip with low-opacity `bgColor` background and `font-semibold` `text-[13px]` name in the agent's `nameColor`. Herman is in `AGENT_CONFIG` (capitalized "Herman", purple-400 name, purple avatar). Sam is in `AGENT_CONFIG` (red avatar) for future transcripts.
    - UAT-12-04: Run page header shows an `<h1>` with the prompt truncated to one line, full prompt as native tooltip on hover, "Untitled run" fallback when prompt is null. Endpoint `GET /api/runs/[id]` enforces tenant isolation.
  </what-built>
  <how-to-verify>
    Re-screenshot each of the original 2026-04-27 UAT screenshots in a fresh browser session and compare:

    1. **Defect 1 — Dark Run History (originally screenshot 091042):**
       - Open https://console.beaglemind.ai/runs in a new incognito / private window.
       - Hard reload (Ctrl/Cmd + Shift + R).
       - Expected: Page background, table, header are all dark. No white panel anywhere on the screen, ever — not on first paint, not after hydration, not on the search input, not on the status filter buttons.
       - If ANY light/white area appears, even briefly, this is NOT closed — capture the offending screenshot and surface to the planner.

    2. **Defect 2 — Writers' Room loading skeleton (originally screenshot 091005):**
       - From the dashboard, start a NEW run on any project (or open a freshly-started run that hasn't received any agent messages yet — `eventOrder` should be empty).
       - Click into the run page.
       - Expected: above the Composer you should see:
         · centered subtitle "Mo, Jarvis, and Herman are getting ready…" (note the unicode ellipsis, not three dots)
         · 3 stacked agent placeholders: Mo (amber avatar "M"), Jarvis (teal avatar "J"), Herman (purple avatar "H")
         · each placeholder has the agent's display name in semibold, role in muted text, italic "thinking…" status, and a pulsing skeleton bar where the message body would be
       - Once the first agent_message arrives via SSE, the skeleton should disappear and the real virtualized transcript takes over.

    3. **Defect 3 — Speaker chips + agent config (originally screenshot 090945):**
       - Open any run that has multiple agents speaking (Mo + Jarvis + Herman).
       - Expected:
         · each speaker name is in a colored chip background (Mo amber tint, Jarvis teal tint, Herman purple tint).
         · Herman's name renders as "Herman" capitalized, NOT "herman" lowercase.
         · the speaker block is visually distinct from the body prose — easy to scan a long thread without re-reading.
         · in Studio mode, open the Process drawer and inspect the Cost section — Herman appears as "Herman" (capitalized) in the per-agent breakdown.

    4. **Defect 4 — Run page title (originally screenshots 090945, 091005):**
       - Open any run.
       - Expected:
         · ABOVE the status badge + "Run" + UUID row, an `<h1>` shows the user's original prompt truncated to one line with ellipsis if it overflows.
         · hover the title — full prompt appears as a native browser tooltip.
         · switch between Writers' Room → Timeline → Boardroom → Canvas tabs and back — title does NOT refetch (no flash, no loading state, deduped via TanStack Query).
         · if the run somehow has a null prompt, the title shows "Untitled run" — no layout shift, just the fallback text.

    Optional cross-checks:
    - Tenant isolation: open a run from a different tenant's URL (if a second tenant exists) and confirm 404 — the title fetch should not leak content.
    - Mobile: open `/runs` on a phone screen size — the dark palette + run rows should still render correctly.

    If any of the four defects is NOT closed, type a description (e.g. "defect 2 — Herman avatar is gray, not purple") so the planner can dispatch a follow-up plan. If all four are closed, type `approved` to advance to Task 3.
  </how-to-verify>
  <resume-signal>Type "approved" to advance to Task 3, or describe any remaining issue (one defect per line).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Update REQUIREMENTS.md traceability — mark UAT-12-01..04 Complete</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    .planning/REQUIREMENTS.md,
    .planning/phases/12-ui-polish-from-phase-11-uat/12-CONTEXT.md
  </read_first>
  <action>
    Open `.planning/REQUIREMENTS.md`. Make TWO sets of edits:

    A) In the `### UI Polish (from Phase 11 UAT)` section near the top of the file, change each requirement checkbox from `[ ]` to `[x]`:
       - `- [ ] **UAT-12-01**:` → `- [x] **UAT-12-01**:`
       - `- [ ] **UAT-12-02**:` → `- [x] **UAT-12-02**:`
       - `- [ ] **UAT-12-03**:` → `- [x] **UAT-12-03**:`
       - `- [ ] **UAT-12-04**:` → `- [x] **UAT-12-04**:`

       Do NOT change the requirement text itself — only the checkbox.

    B) In the `## Traceability` table at the bottom of the file, update the four rows for UAT-12-01..04:
       - `| UAT-12-01 | Phase 12 | Pending |` → `| UAT-12-01 | Phase 12 | Complete |`
       - `| UAT-12-02 | Phase 12 | Pending |` → `| UAT-12-02 | Phase 12 | Complete |`
       - `| UAT-12-03 | Phase 12 | Pending |` → `| UAT-12-03 | Phase 12 | Complete |`
       - `| UAT-12-04 | Phase 12 | Pending |` → `| UAT-12-04 | Phase 12 | Complete |`

    DO NOT:
    - Touch any other row in the traceability table.
    - Touch the `## v2 Requirements`, `## Out of Scope`, or `## Coverage` sections.
    - Change the `Last updated:` footer (it's a manually-maintained timestamp; if you do update it, set to today's date `2026-04-27`).
    - Add a new section claiming the phase is complete — that's the job of the SUMMARY file the orchestrator writes after this plan.
  </action>
  <verify>
    <automated>grep -c "UAT-12-0[1-4] | Phase 12 | Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains `[x] **UAT-12-01**` (verify with `grep`)
    - File contains `[x] **UAT-12-02**` (verify with `grep`)
    - File contains `[x] **UAT-12-03**` (verify with `grep`)
    - File contains `[x] **UAT-12-04**` (verify with `grep`)
    - File traceability table contains the row `| UAT-12-01 | Phase 12 | Complete |` (verify with `grep`)
    - Same for UAT-12-02, UAT-12-03, UAT-12-04 (verify each)
    - File NO LONGER contains the row `| UAT-12-01 | Phase 12 | Pending |` (verify: `grep -c "UAT-12-01 | Phase 12 | Pending" .planning/REQUIREMENTS.md` returns 0)
    - `grep -c "UAT-12-0[1-4] | Phase 12 | Complete" .planning/REQUIREMENTS.md` returns 4
    - No other traceability rows changed (spot-check by running `grep -E "Phase 1[01] | (Complete|Pending)" .planning/REQUIREMENTS.md` — Phase 11 still Complete, Phase 1 entries unchanged)
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md reflects the closed UAT items. Phase 12 is implementation-complete and traceability-complete; the orchestrator will write the phase SUMMARY after this plan finishes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → GitHub | `git push origin main` over authenticated SSH or HTTPS — already trusted in CI/CD path. |
| GitHub → BeagleHQ | `git pull` over SSH from BeagleHQ — already trusted in deploy path established in Phase 1. |
| BeagleHQ → Docker | `docker build` and `docker compose up -d` — already trusted; image is built locally on the VPS, not pulled from a registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-09 | (no new threats — deploy + UAT only) | deploy chain | accept | Deploy chain is unchanged from prior phases. UAT step is a human visual verification with no security surface. REQUIREMENTS.md edit is a docs-only change. |
</threat_model>

<verification>
- `ssh beaglehq 'docker ps --filter name=console-web --format "{{.Status}}"' | grep -q "^Up"` succeeds after deploy
- Lucas confirms all four defects are visually closed against the original 2026-04-27 screenshots
- `grep -c "UAT-12-0[1-4] | Phase 12 | Complete" .planning/REQUIREMENTS.md` returns 4
- ROADMAP.md Phase 12 entry can be flipped to `[x]` by the orchestrator after this plan's SUMMARY is written (out of scope for this plan)
</verification>

<success_criteria>
- All four UAT requirements (UAT-12-01..04) marked Complete in REQUIREMENTS.md
- Production build at console.beaglemind.ai serves the new code (verified via `docker ps` Up status + curl 200/3xx)
- Lucas approves the UAT pass with `approved` resume signal
- No regressions reported during UAT — if regressions are found, this plan exits inconclusive and the planner re-runs with `--gaps` mode
</success_criteria>

<output>
After completion, create `.planning/phases/12-ui-polish-from-phase-11-uat/12-05-SUMMARY.md`
</output>
