---
phase: 07-artifacts-run-history
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/artifacts/[id]/preview/route.ts
  - apps/web/components/transcript/artifact-card.tsx
  - apps/web/components/transcript/artifact-preview-panel.tsx
autonomous: true
requirements: [ARTF-01, ARTF-02, ARTF-03]
must_haves:
  truths:
    - "Artifact cards show filename, size, and a View button alongside Download"
    - "Clicking View on a PDF opens an inline iframe preview in a slide-over panel"
    - "Clicking View on a DOCX renders converted HTML in the same slide-over panel"
    - "DOCX conversion failure gracefully falls back to download link"
    - "Download still works via presigned URL redirect"
  artifacts:
    - path: "apps/web/app/api/artifacts/[id]/preview/route.ts"
      provides: "DOCX-to-HTML conversion endpoint via mammoth"
      exports: ["GET"]
    - path: "apps/web/components/transcript/artifact-preview-panel.tsx"
      provides: "Slide-over panel for inline PDF/DOCX preview"
      min_lines: 60
    - path: "apps/web/components/transcript/artifact-card.tsx"
      provides: "Extended artifact card with View + Download buttons"
      min_lines: 50
  key_links:
    - from: "apps/web/components/transcript/artifact-card.tsx"
      to: "apps/web/components/transcript/artifact-preview-panel.tsx"
      via: "View button opens panel with artifactId + mimeType"
      pattern: "ArtifactPreviewPanel"
    - from: "apps/web/components/transcript/artifact-preview-panel.tsx"
      to: "/api/artifacts/[id]/preview"
      via: "fetch for DOCX HTML conversion"
      pattern: "fetch.*api/artifacts.*preview"
    - from: "apps/web/components/transcript/artifact-preview-panel.tsx"
      to: "/api/artifacts/[id]/download"
      via: "iframe src for PDF preview"
      pattern: "iframe.*api/artifacts.*download"
---

<objective>
Extend artifact cards with inline preview (PDF via iframe, DOCX via mammoth) and a slide-over preview panel.

Purpose: Users need to preview delivered artifacts without downloading — PDF renders natively in an iframe, DOCX converts to HTML server-side. This completes ARTF-01 (bucket-per-tenant already exists via ensureBucket in provisioning), ARTF-02 (enhanced card), and ARTF-03 (download already works, verified here).

Output: Preview API route, preview panel component, extended artifact card.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-artifacts-run-history/07-CONTEXT.md

<interfaces>
<!-- Existing artifact card component to extend -->
From apps/web/components/transcript/artifact-card.tsx:
```typescript
interface ArtifactCardProps {
  event: HubEventEnvelope;
}
// event.content typed as: { filename, mimeType, sizeBytes, artifactId }
```

From apps/web/app/api/artifacts/[id]/download/route.ts:
```typescript
// GET handler: looks up artifact by id in tenant schema, generates MinIO presigned URL (5-min), redirects
// Uses: requireTenantContext(), getTenantDb(), getMinioClient(), GetObjectCommand, getSignedUrl
```

From packages/db/src/minio-client.ts:
```typescript
export function getMinioClient(): S3Client;
export async function ensureBucket(bucketName: string): Promise<void>;
```

From packages/db/src/schema/tenant.ts:
```typescript
// artifacts table: id, runId, filename, mimeType, sizeBytes, minioKey, agentId, createdAt
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DOCX preview API route + install mammoth</name>
  <files>apps/web/app/api/artifacts/[id]/preview/route.ts</files>
  <read_first>
    - apps/web/app/api/artifacts/[id]/download/route.ts (copy pattern exactly)
    - packages/db/src/minio-client.ts (getMinioClient import)
    - apps/web/package.json (add mammoth dependency)
  </read_first>
  <action>
    1. Install mammoth: `cd apps/web && pnpm add mammoth`
    2. Create `apps/web/app/api/artifacts/[id]/preview/route.ts` with `export const runtime = 'nodejs'`
    3. GET handler pattern identical to download/route.ts for auth + artifact lookup:
       - `requireTenantContext()` for tenantId
       - `getTenantDb(tenantId)` to query artifacts table by id
       - Return 404 if not found
    4. Check artifact.mimeType:
       - If `application/pdf`: generate presigned URL (5-min expiry per D-03) and return JSON `{ type: 'pdf', url: presignedUrl }`
       - If `application/vnd.openxmlformats-officedocument.wordprocessingml.document` (docx): fetch the object from MinIO via GetObjectCommand, pipe Body to Buffer, call `mammoth.convertToHtml({ buffer })`, return JSON `{ type: 'docx', html: result.value }`
       - Otherwise: return JSON `{ type: 'unsupported' }`
    5. Wrap in try/catch. On mammoth conversion failure, return `{ type: 'error', message: 'Preview unavailable' }` with 200 (not 500 — frontend handles gracefully per D-05).
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>Preview API route compiles, mammoth installed, PDF returns presigned URL JSON, DOCX returns HTML JSON, unsupported types handled gracefully.</acceptance_criteria>
  <done>GET /api/artifacts/[id]/preview returns type-discriminated JSON for pdf/docx/unsupported with tenant-scoped artifact lookup.</done>
</task>

<task type="auto">
  <name>Task 2: Artifact preview panel + extend artifact card with View button</name>
  <files>apps/web/components/transcript/artifact-preview-panel.tsx, apps/web/components/transcript/artifact-card.tsx</files>
  <read_first>
    - apps/web/components/transcript/artifact-card.tsx (current implementation)
    - apps/web/components/ui/dialog.tsx (for slide-over panel base — or use Sheet if available)
    - apps/web/components/studio/cost-section.tsx (dark theme styling patterns)
  </read_first>
  <action>
    1. Check if shadcn Sheet component exists. If not, install it: `cd apps/web && npx shadcn@latest add sheet`. Sheet is the right primitive for a slide-over panel per D-06.

    2. Create `apps/web/components/transcript/artifact-preview-panel.tsx`:
       - Props: `{ artifactId: string; filename: string; mimeType: string; open: boolean; onOpenChange: (open: boolean) => void }`
       - Uses Sheet (side="right", className for ~60% width on desktop)
       - On open, fetch `/api/artifacts/${artifactId}/preview`
       - Based on response `type`:
         - `pdf`: Render `<iframe src={response.url} className="h-full w-full" />` — browser-native PDF viewer per D-04
         - `docx`: Render `<div dangerouslySetInnerHTML={{ __html: response.html }} className="prose prose-invert max-w-none p-4" />` — mammoth HTML per D-05
         - `unsupported` or `error`: Show message "Preview not available for this file type" with a Download fallback link
       - Show loading skeleton while fetching
       - Sheet header: filename + Download button (link to `/api/artifacts/${artifactId}/download`)

    3. Modify `apps/web/components/transcript/artifact-card.tsx`:
       - Add local state: `const [previewOpen, setPreviewOpen] = useState(false)`
       - Determine if file is previewable: `const canPreview = content.mimeType === 'application/pdf' || content.mimeType === 'application/vnd.openxmlformats-officedocument.wordprocessingml.document'`
       - Add "View" Button before the existing Download button (only if canPreview), onClick sets previewOpen=true per D-06
       - Render `<ArtifactPreviewPanel>` at the bottom of the Card, passing artifactId, filename, mimeType, open, onOpenChange
       - Keep existing Download button unchanged
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>Artifact card shows View button for PDF/DOCX files. Clicking View opens a right slide-over panel. PDF renders in iframe. DOCX renders as converted HTML. Unsupported types show fallback. Download button still works.</acceptance_criteria>
  <done>Artifact cards display View + Download buttons. Slide-over panel renders inline preview for PDF (iframe) and DOCX (mammoth HTML) per D-04, D-05, D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> preview API | User requests preview for artifact ID they may not own |
| MinIO object -> mammoth | Untrusted file content parsed by mammoth library |
| mammoth HTML -> browser | Converted HTML rendered via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | preview API | mitigate | Tenant-scoped artifact lookup via getTenantDb ensures user can only preview their own tenant's artifacts |
| T-07-02 | Information Disclosure | presigned URL | accept | 5-min expiry limits exposure window; URL is per-session, tenant-scoped |
| T-07-03 | Elevation of Privilege | mammoth HTML XSS | mitigate | mammoth produces clean HTML (no script tags); wrap in sanitized container with prose-invert styling. Consider DOMPurify if mammoth output is untrusted. |
</threat_model>

<verification>
1. TypeScript compiles without errors
2. Artifact card renders View button for PDF/DOCX mime types
3. Preview panel opens as slide-over on View click
4. Download route still works (regression check)
</verification>

<success_criteria>
- Artifact cards show View + Download for previewable files, Download-only for others
- PDF preview loads in iframe within slide-over panel
- DOCX preview shows converted HTML in slide-over panel
- Failed conversions show graceful fallback with download link
</success_criteria>

<output>
After completion, create `.planning/phases/07-artifacts-run-history/07-01-SUMMARY.md`
</output>
