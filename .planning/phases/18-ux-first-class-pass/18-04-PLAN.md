# 18-04 — Trust-signal cluster

**Severity:** 🟠 HIGH (H1+H2+H3+H4+H10+M3)
**Est:** 30 min

<objective>
The app currently whispers "internal tool" to discerning customers via:
- Stale "offline" presence dots on agents who just responded
- Sam Sentinel exposed in customer sidebar
- LiteLLM + Grafana admin links in header
- Run-header metadata strip is jargon-dense
- Run History filter pill says lowercase "Agent: mo"
- Failure-bubble visually identical to a real reply

**Output:** Trust signals tightened — app reads as a confident customer-facing product.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Presence semantics + tooltip</name>
  <action>
1. Find sidebar agent component
2. Extend "ready" window from 30min → 6h (or whatever — the stale-dot effect is the issue)
3. Add hover-tooltip on each presence dot: "Live · responding now" / "Ready · responded recently" / "Offline · not connected"
4. If an agent has any successful response in the last hour, force "ready" or higher.
  </action>
  <done>Presence dots have tooltips; agents-active-recently show ready/live not offline.</done>
</task>

<task type="auto">
  <name>Task 2: Hide Sam from non-operator sidebar</name>
  <action>
1. Sam (Sentinel) is internal QA — not customer-facing
2. Filter Sam out of sidebar agent list unless `is_operator` flag is true
3. Sam still visible in Studio mode → Process panel → Sentinel section (already there)
  </action>
  <done>Customer view: 3 agents in sidebar (Mo, Jarvis, Herman). Operator view: 4 agents.</done>
</task>

<task type="auto">
  <name>Task 3: Hide LiteLLM + Grafana from non-operator header</name>
  <action>
1. Same is_operator gate
2. Customer header: just Clean/Studio toggle, Settings, Sign out
3. Operator header: + LiteLLM, Grafana links
  </action>
  <done>Non-operator header is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Run header readability</name>
  <action>
1. Current: `completed · #6c18788a · 2m 44s · $1.06 · 4 agents · 10 events · 12m ago`
2. New: status pill → time-ago primary → details on hover
   - Primary line: `Completed · 12 min ago`
   - Secondary line (smaller, muted): `2m 44s · $1.06 · 3 agents · 10 events · #6c18788a`
3. Each value gets a hover-tooltip with full label ("Run duration: 2 minutes 44 seconds")
4. "4 agents" → "3 agents" (exclude system from count)
  </action>
  <done>Run header is scannable for the most-relevant info first.</done>
</task>

<task type="auto">
  <name>Task 5: Display-name everywhere agentId leaks</name>
  <action>
1. Run History filter pill: `Agent: mo` → `Agent: Mo`
2. KPI sub-labels: "mo only" → "Mo only", "mo per completed run" → "per Mo run", "Filtered by mo" → "Filtered to Mo"
3. Anywhere else `agentId` is rendered raw, swap for `getDisplayName(agentId)`.
  </action>
  <done>No lowercase agent IDs visible to users.</done>
</task>

<task type="auto">
  <name>Task 6: Failure-bubble distinct styling</name>
  <action>
1. Component: agent-message bubble where `metadata.errorKind === 'agent_failure'`
2. Style: italic, muted color (gray-500), smaller text, optional warning icon (exclamation triangle), tighter padding
3. Don't show timestamp or model badge for failure bubbles — they're meta-events
  </action>
  <done>Failure bubble visually distinct from a real reply.</done>
</task>

</tasks>

<success_criteria>
- Presence tooltips visible on hover; dots reflect realistic activity
- Customer sidebar shows 3 agents (no Sam)
- Customer header has no LiteLLM/Grafana
- Run header readable at-a-glance
- "Mo" not "mo" everywhere
- Failure bubble looks like a system note, not a reply
</success_criteria>
