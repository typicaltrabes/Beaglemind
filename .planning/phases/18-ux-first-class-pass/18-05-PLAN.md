# 18-05 — Run-page polish

**Severity:** 🟠 HIGH + 🟡 MEDIUM (H5+H6+H8+H9+M2)
**Est:** 30 min

<objective>
Cluster of run-page polish issues that individually are small but together signal "unfinished":
- H5: Timeline tooltip leaks `agent_message` (DB type) on user-message dot
- H6: Boardroom column hygiene — empty SCENE column, noisy `system` column
- H8: Cancelled runs show non-zero costs without explanation
- H9: Long-prompt run cards on dashboard truncate mid-word
- M2: state_transition events render as inline text paragraphs

**Output:** Run page reads as a polished surface.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Timeline tooltip — humanize event types</name>
  <action>
1. Find Timeline dot tooltip component
2. Map internal event types to display labels:
   - user message → "You said"
   - agent_message → display name + role ("Mo · Senior Partner")
   - state_transition → "Run state changed"
   - sentinel_flag → "Sentinel observation"
   - plan_proposal → "Plan proposed"
3. Tooltip positioning: anchor below dot, not 50px above (use base-ui Tooltip side="bottom" align="center")
  </action>
  <done>No raw type strings in tooltip; tooltip anchored to dot.</done>
</task>

<task type="auto">
  <name>Task 2: Boardroom column hygiene</name>
  <action>
1. Hide SCENE column when all events have no scene (most short runs). Render only when ≥1 scene-bearing event exists.
2. Render `state_transition` events as a thin full-width divider spanning all agent columns (with the timestamp + transition text), NOT as a column entry.
3. Drop the `system` column entirely.
  </action>
  <done>Boardroom shows only meaningful agent columns + transcript-spanning state dividers.</done>
</task>

<task type="auto">
  <name>Task 3: Cancelled-cost tooltip</name>
  <action>
1. Run table cost cell where status === 'cancelled' AND cost > 0:
2. Render: `$9.45ⓘ` with hover tooltip "$9.45 spent before cancellation"
3. If status === 'cancelled' AND cost === 0: render `—`
  </action>
  <done>Cancelled-cost cells are self-explanatory.</done>
</task>

<task type="auto">
  <name>Task 4: Dashboard run card truncation</name>
  <action>
1. (Defer if 18-03 lands first — table replaces cards.)
2. If cards remain anywhere, apply `line-clamp-2` with `text-overflow: ellipsis`.
  </action>
  <done>No mid-word truncation visible.</done>
</task>

<task type="auto">
  <name>Task 5: state_transition divider component</name>
  <action>
1. Replace the `Run transitioned from executing to completed` paragraph in Writers' Room with:
   - Centered horizontal divider line
   - Tiny label in middle: `[icon] Completed · 12 min ago`
2. Subdued color, ~12px font, muted gray.
3. Same component reused in Boardroom (Task 2)
  </action>
  <done>State transitions are visual breakpoints, not paragraphs.</done>
</task>

</tasks>

<success_criteria>
- Timeline tooltips show display labels not type strings
- Boardroom: empty columns hidden, system events as dividers
- Cancelled costs explained
- State transitions visually distinct from messages
</success_criteria>
