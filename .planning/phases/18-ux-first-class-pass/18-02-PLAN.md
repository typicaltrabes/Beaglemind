# 18-02 — Share Replay: finish modal implementation

**Severity:** 🔴 CRITICAL (C2)
**Est:** 30 min

<objective>
The Share Replay modal currently opens with title + caption + close button only. No link, no copy button, no generate button. The /shared-links page exists but is permanently empty because nothing creates entries.

**Output:** Click Share replay → modal generates a tokenized URL, displays it with copy-to-clipboard, persists to shared_links table. /shared-links page shows the new entry with revoke action.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Inspect existing shared-link backend (Phase 8 work)</name>
  <action>
1. grep `apps/web` for `shared_links`, `share-link`, `replay`
2. Check tenant schema for shared_links table — should already exist from Phase 8 (replay-sharing)
3. Find the API route for creating shared links (likely `app/api/runs/[id]/share/route.ts` or similar)
4. Find the modal component (`components/share-replay-dialog.tsx` or similar)
  </action>
  <done>Located: backend route, modal component, table schema.</done>
</task>

<task type="auto">
  <name>Task 2: Wire backend → modal → /shared-links</name>
  <action>
1. Modal mount → call `POST /api/runs/${runId}/share` to mint token (or fetch existing token if one exists for this run)
2. Display the URL: `https://console.beaglemind.ai/replay/${token}` with copy-to-clipboard button
3. Show "Anyone with this link can view a read-only replay" caption (already there)
4. Show "Revoke link" button that calls `DELETE /api/runs/${runId}/share`
5. After revoke: hide the URL, show "No active link for this run"
6. /shared-links page should render the same list with per-row revoke action
  </action>
  <verify>
    <automated>Playwright: open Share modal → assert URL element visible → click Copy → assert clipboard contains URL pattern. Visit /shared-links → assert row exists.</automated>
  </verify>
  <done>Modal generates + shows + revokes share URL; /shared-links lists active shares.</done>
</task>

<task type="auto">
  <name>Task 3: Verify recipient view</name>
  <action>
Open the shared URL in a separate browser context (or incognito). Should:
- Render run transcript read-only
- No composer
- No Studio toggle
- Header should not say "Beagle Agent Console" with Sign Out — should be a minimal external view
- If a Phase-8 replay view exists, link to it from the share URL
  </action>
  <done>Recipient view loads without auth and shows the conversation read-only.</done>
</task>

</tasks>

<success_criteria>
- Click Share replay → URL appears in modal
- Copy button works (clipboard contains URL)
- /shared-links shows the new link
- Recipient (no auth) sees read-only replay
- Revoke removes the link from /shared-links AND breaks the recipient URL
</success_criteria>
