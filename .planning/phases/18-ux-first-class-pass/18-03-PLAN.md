# 18-03 — Project dashboard parity with /runs

**Severity:** 🔴 CRITICAL (C3)
**Est:** 30 min

<objective>
The page customers land on after picking a project (`/projects/[id]`) shows a worse view of the same data than `/runs`:
- No KPI strip
- No search bar
- No status filter chips
- Cards instead of table — long prompts truncate mid-word
- Each "completed" pill is visual noise

The good version exists at `/runs` but is hidden behind a tiny history icon in the bottom-left footer.

**Output:** Project dashboard renders the same KPI strip + searchable filter-able table as /runs, scoped to that project. The composer + project header stays at the top.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Extract /runs table + KPI strip into a shared component</name>
  <action>
1. Find `apps/web/app/(dashboard)/runs/page.tsx` and identify:
   - KPI strip component
   - Search input
   - Status filter chips
   - Table component
2. Extract them into `apps/web/components/run-list/` shared module:
   - `kpi-strip.tsx` (props: tenantId, projectId?, agentId?)
   - `run-search-filter.tsx`
   - `run-table.tsx`
3. /runs page becomes a thin wrapper passing all-projects scope.
  </action>
  <done>Shared components exist; /runs page still works identically.</done>
</task>

<task type="auto">
  <name>Task 2: Replace project-dashboard `Runs` section with shared components</name>
  <action>
1. In `apps/web/app/(dashboard)/projects/[id]/page.tsx`:
   - Keep heading + composer
   - Replace the `Runs` section + card list with `<KpiStrip projectId={id} />` + `<RunSearchFilter projectId={id} />` + `<RunTable projectId={id} />`
2. KPI strip on project dashboard scopes to this project (Total Runs in this project, Total Spend, etc.)
3. Search filters runs in this project only.
  </action>
  <verify>
    <automated>Playwright: visit `/projects/d1f573db-9d66-438f-bc18-be71a5a83b3d` → assert KPI strip with Total Runs visible → assert table headers Project/Title/Status/Cost/Duration/Artifacts/Date</automated>
  </verify>
  <done>Project dashboard mirrors /runs experience, project-scoped.</done>
</task>

</tasks>

<success_criteria>
- KPI strip on project dashboard (Total Runs / Spend / Avg / Today)
- Search bar functional
- Status filter chips functional
- Table view (not cards) with same columns as /runs
- Long-prompt truncation handled with ellipsis (line-clamp-2 fallback)
</success_criteria>
