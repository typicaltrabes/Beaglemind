# 18-07 — Mobile + relative-time polish

**Severity:** 🟡 MEDIUM (M11+M12+M1)
**Est:** 20 min

<objective>
Two mobile fixes + one cross-platform polish that affect first-impression scanability:
- M11: Mobile run-header truncates ("10 e..." cut off; Studio button hidden)
- M12: Mobile message bubbles overflow horizontally — text cut off at right edge
- M1: Dashboard run cards show full localized timestamp; should show relative ("11m ago") with absolute on hover

**Output:** App works smoothly on iPhone-class viewport; timestamps are scannable.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Mobile run-header</name>
  <action>
1. Run header at <768px:
   - Top row: title + status pill (existing)
   - Second row: time-ago + cost (primary)
   - Tap-to-expand: full metadata (agents, events, runId) on tap
2. Or simpler: drop runId + agent count from default mobile view; full strip available via Process panel
  </action>
  <done>Mobile run header fits viewport with no horizontal overflow.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile bubble word-break</name>
  <action>
1. Find agent-message + user-message bubble components
2. Add `max-width: 100%` + `overflow-wrap: break-word` + `word-break: break-word`
3. URLs / long unbreakable strings should wrap not overflow
  </action>
  <done>No bubble text cut off at viewport edge.</done>
</task>

<task type="auto">
  <name>Task 3: Relative timestamps</name>
  <action>
1. Use `Intl.RelativeTimeFormat` or existing util in /runs table
2. Apply to dashboard run cards (project page) — already done in /runs table
3. Format: "11m ago", "2h ago", "1d ago", "3w ago"; older than 30 days falls back to absolute
4. Hover tooltip: full localized timestamp
  </action>
  <done>Dashboard timestamps relative + hover-absolute.</done>
</task>

</tasks>

<success_criteria>
- iPhone 14 viewport (390×844): no horizontal overflow on any run-page surface
- Bubble content wraps cleanly
- Timestamps human-friendly with full info on hover
</success_criteria>
