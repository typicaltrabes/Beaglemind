---
phase: 08-replay-sharing
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - apps/web/app/(dashboard)/shared-links/page.tsx
  - apps/web/components/share/shared-links-table.tsx
  - apps/web/components/share/replay-audit-log.tsx
  - apps/web/app/(dashboard)/layout.tsx
autonomous: false
requirements: [REPL-02, REPL-05]

must_haves:
  truths:
    - "Tenant admin can see all active share links in a management page"
    - "Tenant admin can revoke any share link from the management page"
    - "Tenant admin can see replay view audit logs (who viewed, when, IP)"
    - "Revoked links immediately stop working"
  artifacts:
    - path: "apps/web/app/(dashboard)/shared-links/page.tsx"
      provides: "Share link management page"
    - path: "apps/web/components/share/shared-links-table.tsx"
      provides: "Table of share links with revoke action"
    - path: "apps/web/components/share/replay-audit-log.tsx"
      provides: "Audit log table showing replay views"
  key_links:
    - from: "apps/web/components/share/shared-links-table.tsx"
      to: "/api/share-links"
      via: "GET to list, DELETE to revoke"
      pattern: "fetch.*api/share-links"
    - from: "apps/web/components/share/replay-audit-log.tsx"
      to: "/api/share-links/[id]/views"
      via: "GET audit log for specific link"
      pattern: "fetch.*api/share-links.*views"
---

<objective>
Build the shared links management page with revocation and audit log views. Tenant admins can see all active links, revoke them, and view who accessed each replay.

Purpose: Governance and control -- tenant admins must be able to revoke access and audit who viewed shared content.
Output: Shared links management page, revoke functionality, audit log component, sidebar navigation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-replay-sharing/08-CONTEXT.md
@.planning/phases/08-replay-sharing/08-01-SUMMARY.md
@.planning/phases/08-replay-sharing/08-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->
API: GET /api/share-links -> ShareLink[] (with run info)
API: DELETE /api/share-links/[id] -> revokes link

<!-- From Plan 01 schema -->
shareLinks: { id, runId, token, createdBy, expiresAt, revokedAt, createdAt }
replayViews: { id, shareLinkId, viewerIp, userAgent, viewedAt }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit log API and shared links management page</name>
  <files>apps/web/app/api/share-links/[id]/views/route.ts, apps/web/app/(dashboard)/shared-links/page.tsx, apps/web/components/share/shared-links-table.tsx, apps/web/components/share/replay-audit-log.tsx</files>
  <read_first>apps/web/app/api/share-links/route.ts, apps/web/app/api/share-links/[id]/route.ts, packages/db/src/schema/tenant.ts, apps/web/app/(dashboard)/layout.tsx</read_first>
  <action>
1. Create `apps/web/app/api/share-links/[id]/views/route.ts` (per D-11):
   - GET handler: requireTenantContext(), validate share link belongs to tenant
   - Query replayViews WHERE shareLinkId = id, ORDER BY viewedAt DESC, LIMIT 100
   - Return array: { viewerIp, userAgent, viewedAt }[]
   - Use `export const runtime = 'nodejs'` and `export const dynamic = 'force-dynamic'`

2. Create `apps/web/components/share/shared-links-table.tsx` (per D-08):
   - Client component. Fetches GET /api/share-links on mount
   - Table with columns: Run (truncated runId or project name if available), Created (relative time), Expires (date), Status (active/expired/revoked badge), Views (count), Actions
   - Status derived: if revokedAt set -> "Revoked" (red), if expiresAt < now -> "Expired" (yellow), else "Active" (green)
   - Actions column: "Revoke" button (calls DELETE /api/share-links/[id]), "View log" button (opens audit log)
   - Revoke button: confirm dialog before revoking. After revoke, optimistically update the row
   - "View log" button: expands a row or opens a sheet/dialog showing ReplayAuditLog

3. Create `apps/web/components/share/replay-audit-log.tsx` (per D-11):
   - Client component. Props: { shareLinkId: string }
   - Fetches GET /api/share-links/[id]/views
   - Simple table: Viewer IP, User Agent (truncated), Viewed At (formatted datetime)
   - Empty state: "No views yet"

4. Create `apps/web/app/(dashboard)/shared-links/page.tsx`:
   - Dashboard page rendering SharedLinksTable
   - Page title: "Shared Links"
   - Brief description: "Manage replay share links for your organization"
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && npx tsc --noEmit --project apps/web/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>Shared links page lists all links with status; revoke button works; audit log shows view history</acceptance_criteria>
  <done>Shared links management page shows all share links with status badges. Admin can revoke links. Audit log shows viewer IP, user-agent, and timestamp for each replay view.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete replay sharing flow</name>
  <files>none</files>
  <action>
Human verification of the complete replay sharing system across all three plans. Present the verification steps to the user and wait for approval.

What was built:
- Share link generation from completed runs (Plan 01)
- Public replay page with server-side Clean-mode filtering (Plan 02)
- Link management with revocation and audit logging (Plan 03)
  </action>
  <verify>
    <automated>echo "Checkpoint: manual verification required"</automated>
  </verify>
  <done>User has verified: (1) share button on completed runs generates tokenized URL, (2) public replay shows read-only Clean-mode transcript without auth, (3) shared links page shows all links with revoke and audit log, (4) revoked links show error page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated admin -> management API | Only tenant members can view/revoke their own share links |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-10 | Elevation of Privilege | GET /api/share-links/[id]/views | mitigate | requireTenantContext() + validate link belongs to tenant |
| T-08-11 | Information Disclosure | audit log viewer IP | accept | IPs shown only to tenant admin; logged for security audit purposes |
</threat_model>

<verification>
- Shared links page loads and shows all links
- Revoke button sets revokedAt and link stops working
- Audit log shows view entries
- Sidebar has "Shared Links" nav item
</verification>

<success_criteria>
Tenant admin can manage all share links: view status, revoke access, and audit who viewed replays. Revoked links immediately return an error page to external viewers.
</success_criteria>

<output>
After completion, create `.planning/phases/08-replay-sharing/08-03-SUMMARY.md`
</output>
