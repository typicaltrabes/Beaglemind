---
phase: 11-run-view-tabs-writers-room-timeline-boardroom-canvas
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified: []
autonomous: false
requirements:
  - VIEW-01
  - VIEW-02
  - VIEW-03
must_haves:
  truths:
    - "Phase 11 changes are pushed to origin/main"
    - "A fresh console-web Docker image is built on beaglehq from the updated main"
    - "console.beaglemind.ai is serving the new image with the 4-tab run view"
    - "User has manually verified each of the 4 tabs on a real run"
  artifacts: []
  key_links:
    - from: "origin/main"
      to: "ghcr.io/typicaltrabes/console-web:latest"
      via: "sudo docker build -t ghcr.io/typicaltrabes/console-web:latest -f apps/web/Dockerfile ."
      pattern: "docker build"
    - from: "ghcr.io/typicaltrabes/console-web:latest"
      to: "console.beaglemind.ai"
      via: "sudo docker compose up -d --force-recreate console-web"
      pattern: "docker compose up"
---

<objective>
Ship Phase 11 to production on console.beaglemind.ai using the existing manual deploy workflow. Verify all four tabs end-to-end on a real run before marking the phase complete.

Purpose: Plans 01–04 are merged to local main; users can't use them until they're live on the VPS. One deploy at the end of the phase (not per-plan) per CONTEXT.md.

Output: Running image on beaglehq with the 4-tab run view, plus a user UAT checkpoint confirming behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-CONTEXT.md

Deploy workflow (from CONTEXT.md §Deploy):
1. `git push origin main` (from laptop)
2. `ssh beaglehq 'cd /tmp/beagle-build && sudo git pull origin main'`
3. `ssh beaglehq 'cd /tmp/beagle-build && sudo docker build -t ghcr.io/typicaltrabes/console-web:latest -f apps/web/Dockerfile .'`
4. `ssh beaglehq 'cd /opt/beagle-console && sudo docker compose up -d --force-recreate console-web'`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push main and trigger a fresh image build + redeploy on beaglehq</name>
  <files></files>
  <read_first>
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-CONTEXT.md,
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-01-PLAN.md,
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-02-PLAN.md,
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-03-PLAN.md,
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-04-PLAN.md
  </read_first>
  <action>
    Execute the CONTEXT.md-sanctioned deploy sequence in order. Do NOT improvise or introduce new infra (no new CI workflow, no compose file edits, no Caddy config changes).

    A) From the laptop repo root, verify all Phase 11 plan SUMMARYs exist and all Phase 11 work is committed:
       ```
       git status            # expect clean tree
       git log --oneline -20  # expect plans 01–04 commits present
       ```
       If anything is uncommitted, STOP and surface it — the deploy step below assumes `origin/main` is up to date.

    B) Push main to GitHub:
       ```
       git push origin main
       ```

    C) SSH to beaglehq and pull the latest code into the build directory:
       ```
       ssh beaglehq 'cd /tmp/beagle-build && sudo git pull origin main'
       ```
       Capture the commit SHA printed by git pull for the summary.

    D) Build a fresh `console-web` image tagged `latest`:
       ```
       ssh beaglehq 'cd /tmp/beagle-build && sudo docker build -t ghcr.io/typicaltrabes/console-web:latest -f apps/web/Dockerfile .'
       ```
       This is the only build command — do NOT build agent-hub or worker (Phase 11 is frontend-only).

    E) Roll the image on the running stack:
       ```
       ssh beaglehq 'cd /opt/beagle-console && sudo docker compose up -d --force-recreate console-web'
       ```

    F) Verify the service is up:
       ```
       ssh beaglehq 'sudo docker compose -p beagle-console ps console-web'
       curl -sS -o /dev/null -w "%{http_code}\n" https://console.beaglemind.ai/
       ```
       Both should indicate a healthy service (container "Up", HTTP 200 or 307).

    Capture all command outputs into the SUMMARY.md for traceability. If any step fails, STOP and return the exact error — do not retry more than once for transient failures.
  </action>
  <verify>
    <automated>curl -sS -o /dev/null -w "%{http_code}\n" https://console.beaglemind.ai/ | grep -E "^(200|301|302|307)$"</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin main` completed without rejection
    - `git pull origin main` on beaglehq output ends with a commit hash that matches local HEAD (verify by comparing `git rev-parse HEAD` locally with pulled hash on server)
    - `docker build` exited 0 on beaglehq
    - `docker compose up -d --force-recreate console-web` output shows `Started` or `Recreate` for console-web
    - `docker compose ps console-web` shows state `Up` (or `running`)
    - `curl https://console.beaglemind.ai/` returns 200/301/302/307 (not 5xx, not connection-refused)
  </acceptance_criteria>
  <done>
    Fresh console-web image from the current main branch is serving https://console.beaglemind.ai/ with no container errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human UAT of the 4 tabs on console.beaglemind.ai</name>
  <files></files>
  <read_first>
    .planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-CONTEXT.md
  </read_first>
  <action>
    Pause execution and present the verification checklist below to the user. Do not proceed past this checkpoint until the user replies with `approved` or describes issues to fix.
  </action>
  <what-built>
    4-tab run view deployed to console.beaglemind.ai:
    - Writers' Room (default; existing transcript)
    - Timeline (horizontal scrubbable replay with scene dividers + detail panel)
    - Boardroom (parallel agent columns; mobile accordion)
    - Canvas (artifact-first with proximity margin comments)
  </what-built>
  <how-to-verify>
    Open a browser logged into console.beaglemind.ai and run through this 10-point check on any project with at least one completed run that has artifacts:

    1. Navigate to `/projects/:p/runs/:r` for a run with ≥5 events and ≥1 delivered artifact.
    2. Confirm the 4 tab buttons are visible: "Writers' Room", "Timeline", "Boardroom", "Canvas". Writers' Room should be selected by default.
    3. Confirm the transcript, TLDR banner, and Composer at the bottom are all unchanged from what you saw before this phase.
    4. Click "Timeline". Confirm:
       - URL becomes `…?view=timeline`
       - A horizontal lane of colored dots appears, one per event
       - Hovering a dot shows agent/type/preview tooltip
       - Clicking a dot opens a detail panel below with the same rendering as Writers' Room
       - The range slider below the lane can be dragged and the detail panel follows the playhead
       - Scene names appear above the lane where scenes start
       - Reloading the page keeps Timeline active
    5. Toggle Studio mode (header Studio switch). Confirm sentinel_flag and state_transition dots now appear on the Timeline.
    6. Click "Boardroom". Confirm:
       - URL becomes `…?view=boardroom`
       - One column per distinct agent (at least Mo, Jarvis, user, possibly Sentinel)
       - Each column scrolls independently
       - In Studio mode, state_transition rows appear inside EACH column
       - Switching back to Clean mode drops state_transition from every column
    7. Resize the browser to <768px or open a mobile preview. Confirm the Boardroom collapses into a single-column accordion with agent section headers.
    8. Click "Canvas". Confirm:
       - URL becomes `…?view=canvas`
       - The selected artifact renders inline on the left (PDF iframe or rendered DOCX)
       - A comment rail on the right shows up to 5 agent messages labeled before/at/after
       - If the run has >1 artifact, a pill strip at the top lets you switch between them
    9. Click the same dot twice on Timeline — detail panel toggles off. Same for clicking the Close button.
    10. Navigate to a run with 0 events and confirm Timeline shows "Run has not started yet" and Canvas shows "No artifacts delivered yet…".

    If ALL 10 checks pass → reply `approved`. Otherwise reply with the specific failure (tab + step + observed behavior + any console errors).
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification only; no automated replacement for browser UAT</automated>
  </verify>
  <acceptance_criteria>
    - User replies with the literal string `approved`
    - OR user reports issues which are fixed in a follow-up gap-closure plan before proceeding
  </acceptance_criteria>
  <done>
    User has signed off that all 4 tabs behave per the 10-point checklist on console.beaglemind.ai.
  </done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Laptop → GitHub → beaglehq | Existing deploy pipeline; no new boundary introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Tampering (bad image promoted to production) | docker build/compose step | mitigate | Image tag is deterministic (`ghcr.io/typicaltrabes/console-web:latest`) and built from the just-pulled commit; deploy only fires after `git pull` succeeds. Any build failure halts the sequence — `docker compose up` is not attempted on build error. |
| T-11-14 | DoS (force-recreate takes web app offline briefly) | console-web container restart | accept | Expected 2–5 seconds of 502 during recreate; Caddy handles reconnect. Scheduled outside peak usage. |
</threat_model>

<verification>
- `curl https://console.beaglemind.ai/` returns a non-5xx status
- Human UAT checkpoint "approved" received
</verification>

<success_criteria>
- All 4 tabs observable on console.beaglemind.ai on at least one real run
- No regressions in existing Writers' Room / Composer / Studio drawer behavior
- User reports approval via the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/11-run-view-tabs-writers-room-timeline-boardroom-canvas/11-05-SUMMARY.md` with the build SHA, deploy timestamp, and the user's UAT checkpoint response.
</output>
