---
phase: 16-visual-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/agent-config.ts
autonomous: true
requirements:
  - UAT-16-04
must_haves:
  truths:
    - "AGENT_CONFIG.mo.role === 'Governance'"
    - "AGENT_CONFIG.jarvis.role === 'Commercial Risk'"
    - "AGENT_CONFIG.herman.role === 'Stress-Test'"
    - "AGENT_CONFIG.sam.role === 'Sentinel'"
    - "AGENT_CONFIG.user.role === '' (unchanged)"
    - "AGENT_CONFIG.sentinel entry remains intact (legacy id, unchanged role)"
    - "All other AGENT_CONFIG fields (displayName, bgColor, textOnBg, nameColor, initial) remain byte-for-byte identical to pre-change values"
  artifacts:
    - path: "apps/web/lib/agent-config.ts"
      provides: "Updated agent role strings; rest of config unchanged"
  key_links:
    - from: "apps/web/lib/agent-config.ts"
      to: "every getAgentConfig().role consumer"
      via: "single source of truth"
      pattern: "getAgentConfig"
---

<objective>
Track 4 of Phase 16: rebrand the four core agents' role labels per CONTEXT.md `<decisions>` Track 4. This is a config-only change with zero downstream code edits — every consumer already reads `getAgentConfig(...).role` and will pick up the new strings automatically.

Per CONTEXT.md `<decisions>` Track 4 verbatim:

| agent   | old role               | new role             |
| ------- | ---------------------- | -------------------- |
| mo      | `workflow lead`        | `Governance`         |
| jarvis  | `research analyst`     | `Commercial Risk`    |
| herman  | `open-weight researcher` | `Stress-Test`      |
| sam     | `sentinel`             | `Sentinel`           |
| user    | `''`                   | `''` (unchanged)     |

Per `<specifics>`: external-feedback-driven brand differentiation. The single most-impactful change in the phase per the feedback memo.

This plan is in Wave 1 (independent — only modifies one file). Trivially small, isolated. The legacy `sentinel` entry in AGENT_CONFIG (with role 'quality monitor') is left unchanged — it's a separate id from `sam` and may still be referenced by historical events; only the four currently-shipping agents get the rebrand.

Output:
- Modified `apps/web/lib/agent-config.ts` — four `role:` string changes only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-visual-overhaul/16-CONTEXT.md

@apps/web/lib/agent-config.ts

<interfaces>
<!-- Extracted from codebase. -->

From apps/web/lib/agent-config.ts (CURRENT — TARGET for 4 string-value edits):
```typescript
export interface AgentConfig {
  displayName: string;
  role: string;
  bgColor: string;
  textOnBg: string;
  nameColor: string;
  initial: string;
}

export const AGENT_CONFIG: Record<string, AgentConfig> = {
  mo:      { displayName: 'Mo',      role: 'workflow lead',          ... },  // → 'Governance'
  jarvis:  { displayName: 'Jarvis',  role: 'research analyst',       ... },  // → 'Commercial Risk'
  sentinel:{ displayName: 'Sentinel',role: 'quality monitor',        ... },  // UNCHANGED (legacy id)
  user:    { displayName: 'You',     role: '',                       ... },  // UNCHANGED
  herman:  { displayName: 'Herman',  role: 'open-weight researcher', ... },  // → 'Stress-Test'
  sam:     { displayName: 'Sam',     role: 'sentinel',               ... },  // → 'Sentinel'
};
```

Every speaker chip (AgentMessage), sidebar agent row (Plan 16-02 AgentRow), cost section, and interrupt button uses `getAgentConfig(id).role` — they will all show the new strings without further edits.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebrand four agent role strings</name>
  <files>
    apps/web/lib/agent-config.ts
  </files>
  <read_first>
    apps/web/lib/agent-config.ts,
    .planning/phases/16-visual-overhaul/16-CONTEXT.md
  </read_first>
  <action>
    Edit `apps/web/lib/agent-config.ts` and update exactly four `role:` string values inside the `AGENT_CONFIG` object. Make NO other changes — every other field (displayName, bgColor, textOnBg, nameColor, initial), every other entry (`sentinel`, `user`, the DEFAULT_CONFIG, the `getAgentConfig` function), and the `AgentConfig` interface MUST remain byte-for-byte identical.

    | agent key | new `role:` value (exact string) |
    | --- | --- |
    | `mo` | `'Governance'` |
    | `jarvis` | `'Commercial Risk'` |
    | `herman` | `'Stress-Test'` |
    | `sam` | `'Sentinel'` |

    DO NOT:
    - Change the order of keys in `AGENT_CONFIG`.
    - Modify the `sentinel` (legacy id) entry — its `role: 'quality monitor'` stays. (CONTEXT.md only lists the four currently-shipping agents to rebrand. The `sentinel` id may still appear on historical events; preserving its display avoids retroactive label drift.)
    - Modify the `user` entry's empty string role.
    - Add or remove any other entries.
    - Touch the `AgentConfig` interface or the `DEFAULT_CONFIG` constant or the `getAgentConfig` function.
    - Capitalize the values differently than specified — `'Stress-Test'` has the hyphen and the capital S+T; `'Commercial Risk'` has the space and two capitals.

    Confirm by running the verify step.
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File contains the literal `'Governance'` (verify with `grep`)
    - File contains the literal `'Commercial Risk'` (verify with `grep`)
    - File contains the literal `'Stress-Test'` (verify with `grep`)
    - File contains the literal `'Sentinel'` as a value of a `role:` field (verify: `grep "role: 'Sentinel'" apps/web/lib/agent-config.ts` returns ≥ 1 match for the `sam` entry)
    - File no longer contains `'workflow lead'`, `'research analyst'`, or `'open-weight researcher'` (verify each: each grep returns NO matches)
    - File still contains `'quality monitor'` (verify — `sentinel` legacy entry preserved)
    - File still contains `displayName: 'Mo'`, `displayName: 'Jarvis'`, `displayName: 'Herman'`, `displayName: 'Sam'`, `displayName: 'You'` (verify each — display names unchanged)
    - File still contains `bgColor: 'bg-[#f7b733]'` for Mo (verify — colors unchanged)
    - File still exports `AGENT_CONFIG`, `getAgentConfig`, `AgentConfig` (verify each)
    - `cd apps/web && pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Four role strings updated; everything else byte-identical. Speaker chips and sidebar rows pick up new labels on next render.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure config string change; no data flow, no API surface, no trust boundary touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04-01 | (none) | string-only constant | accept | The change is four string literals embedded in a const object. No execution context, no user input, no network, no DB. There is no STRIDE category that applies non-trivially. |
</threat_model>

<verification>
- `cd apps/web && pnpm exec tsc --noEmit` — exits 0
- (Manual UAT, deferred to plan 16-06): Open any run page → speaker chip under Mo says "Mo · Governance" instead of "Mo · workflow lead". Sidebar agent rows (after Plan 16-02 lands) show the new role labels.
</verification>

<success_criteria>
- UAT-16-04 closed: agent roles rebranded to Governance / Commercial Risk / Stress-Test / Sentinel.
- No regressions to any other AGENT_CONFIG field.
- No DB migrations, no new dependencies, no consumer code edits required.
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-overhaul/16-04-SUMMARY.md`
</output>
