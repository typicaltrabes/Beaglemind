---
phase: 04-research-sprint-workflow
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - apps/web/app/(dashboard)/layout.tsx
  - apps/web/app/(dashboard)/page.tsx
  - apps/web/app/(dashboard)/projects/[projectId]/page.tsx
  - apps/web/app/(dashboard)/projects/[projectId]/runs/[runId]/page.tsx
  - apps/web/components/sidebar/project-list.tsx
  - apps/web/components/sidebar/question-queue.tsx
  - apps/web/components/sidebar/new-project-dialog.tsx
autonomous: true
requirements: [WORK-01, WORK-02, WORK-05, WORK-06]

must_haves:
  truths:
    - "Dashboard layout has sidebar with project list and question queue"
    - "User can create a new project via dialog in sidebar"
    - "Clicking a project navigates to project page showing runs"
    - "User can start a new run from project page"
    - "Sidebar shows unanswered question count badge"
    - "Clicking a question in sidebar navigates to the run transcript"
  artifacts:
    - path: "apps/web/app/(dashboard)/layout.tsx"
      provides: "Dashboard layout with sidebar + QueryProvider wrapper"
      contains: "QueryProvider"
    - path: "apps/web/components/sidebar/project-list.tsx"
      provides: "Project list component in sidebar"
      exports: ["ProjectList"]
    - path: "apps/web/components/sidebar/question-queue.tsx"
      provides: "Question queue sidebar section with badge"
      exports: ["QuestionQueue"]
    - path: "apps/web/app/(dashboard)/projects/[projectId]/page.tsx"
      provides: "Project detail page with run list and new run form"
      contains: "useStartRun"
  key_links:
    - from: "apps/web/components/sidebar/project-list.tsx"
      to: "/api/projects"
      via: "useProjects() hook"
      pattern: "useProjects"
    - from: "apps/web/app/(dashboard)/projects/[projectId]/page.tsx"
      to: "/api/runs"
      via: "useStartRun() hook"
      pattern: "useStartRun"
---

<objective>
Build the dashboard layout with sidebar (project list + question queue) and the project/run page routing. User can create projects, view runs per project, and start new research sprints.

Purpose: This is the navigation shell and project management layer. The transcript view (Plan 06) plugs into the run page.
Output: Working sidebar + project pages + run page shell
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-research-sprint-workflow/04-CONTEXT.md
@.planning/phases/04-research-sprint-workflow/04-RESEARCH.md

<interfaces>
From apps/web/lib/hooks/use-projects.ts (Plan 04):
```typescript
export function useProjects(): UseQueryResult<Project[]>;
export function useCreateProject(): UseMutationResult;
export function useRuns(projectId: string): UseQueryResult<Run[]>;
```

From apps/web/lib/hooks/use-run-actions.ts (Plan 04):
```typescript
export function useStartRun(): UseMutationResult;
```

From apps/web/lib/stores/ui-store.ts (Plan 04):
```typescript
export const useUIStore: { activeProjectId, activeRunId, setActiveProject, setActiveRun, sidebarOpen, toggleSidebar };
```

From apps/web/lib/stores/run-store.ts (Plan 04):
```typescript
export const useRunStore: { unansweredQuestions: HubEventEnvelope[] };
```

From apps/web/components/providers/query-provider.tsx (Plan 04):
```typescript
export function QueryProvider({ children }: { children: React.ReactNode }): JSX.Element;
```

shadcn/ui components available (Plan 01):
button, card, input, textarea, badge, dialog, scroll-area, separator, skeleton, tooltip
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dashboard layout with sidebar + QueryProvider</name>
  <files>apps/web/app/(dashboard)/layout.tsx, apps/web/app/(dashboard)/page.tsx, apps/web/components/sidebar/project-list.tsx, apps/web/components/sidebar/question-queue.tsx, apps/web/components/sidebar/new-project-dialog.tsx</files>
  <read_first>
    - apps/web/app/(dashboard)/layout.tsx (current layout -- extend with sidebar)
    - apps/web/app/(dashboard)/page.tsx (current placeholder -- replace)
    - apps/web/app/(dashboard)/logout-button.tsx (existing component reference)
    - .planning/phases/04-research-sprint-workflow/04-CONTEXT.md (D-19, D-21: sidebar layout)
  </read_first>
  <action>
**1. Update Dashboard Layout: apps/web/app/(dashboard)/layout.tsx** (per D-21)

The layout is a server component. Wrap children with QueryProvider (client component). Add a sidebar structure.

Structure:
```
<div className="min-h-screen bg-bg flex">
  <DashboardSidebar /> <!-- client component -->
  <div className="flex-1 flex flex-col">
    <header> ... existing header with Beagle Agent Console title + LogoutButton ... </header>
    <main className="flex-1">{children}</main>
  </div>
</div>
```

Create a `DashboardSidebar` client wrapper component (can be inline or separate file) that imports and uses `QueryProvider` to wrap itself:

Actually, simpler: Wrap the entire layout children in QueryProvider. The layout stays a server component -- QueryProvider is a client component wrapper.

```tsx
import { QueryProvider } from '@/components/providers/query-provider';

export default async function DashboardLayout({ children }) {
  await requireTenantContext();
  return (
    <QueryProvider>
      <div className="min-h-screen bg-bg flex">
        <Sidebar />
        <div className="flex-1 flex flex-col">
          <header>...</header>
          <main className="flex-1">{children}</main>
        </div>
      </div>
    </QueryProvider>
  );
}
```

The `<Sidebar>` is a client component imported from `@/components/sidebar/sidebar.tsx`. Create this wrapper that renders ProjectList + QuestionQueue.

Actually, keep sidebar components separate and compose in layout:

Create `apps/web/components/sidebar/sidebar.tsx` ('use client'):
- Renders a `<aside>` with width ~260px, border-r, bg-bg, flex flex-col
- Contains: ProjectList, Separator, QuestionQueue
- Fixed height with ScrollArea for project list

**2. ProjectList: apps/web/components/sidebar/project-list.tsx** (per D-19)

'use client' component:
- Uses `useProjects()` hook to fetch projects
- Renders list of project names as links: `<Link href={/projects/${project.id}}>`
- Active project highlighted (use `usePathname()` to detect active)
- "New Project" button at top opens NewProjectDialog
- Loading state: show Skeleton components
- Empty state: "No projects yet" message

**3. NewProjectDialog: apps/web/components/sidebar/new-project-dialog.tsx** (per D-19)

'use client' component:
- Dialog with form: name (Input, required), description (Textarea, optional)
- Submit calls `useCreateProject()` mutation
- On success: close dialog, router.push to new project page
- Button styled as secondary/outline in sidebar

**4. QuestionQueue: apps/web/components/sidebar/question-queue.tsx** (per D-14)

'use client' component:
- Uses `useRunStore((s) => s.unansweredQuestions)` to get pending questions
- Shows "Questions" header with Badge showing count
- Lists each question: agent name, truncated question text
- Click navigates to the run transcript (Link to `/projects/{projectId}/runs/{runId}`)
- If no unanswered questions, show nothing or "No pending questions"

**5. Update page.tsx: apps/web/app/(dashboard)/page.tsx**

Replace placeholder with redirect to projects list or show a project overview. Simplest: `redirect('/projects')` -- but /projects doesn't have a page. Instead, show a simple welcome screen that lists projects using `useProjects()` as a client component, or redirect to first project. Actually simplest: make it a server component that fetches projects and shows them, with a "Create your first project" CTA if none exist.

For now: make it a client component that uses useProjects() and either shows the project list directly (no sidebar duplication) or redirects. Simplest approach: this page shows "Select a project from the sidebar or create a new one."
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && pnpm tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -5 && grep -c "QueryProvider" apps/web/app/\(dashboard\)/layout.tsx && test -f apps/web/components/sidebar/project-list.tsx && echo "project-list EXISTS" && test -f apps/web/components/sidebar/question-queue.tsx && echo "question-queue EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard layout wraps children in QueryProvider
    - Dashboard layout has sidebar + main area flex layout
    - ProjectList component uses useProjects() hook and renders project links
    - NewProjectDialog opens dialog with name + description form, calls useCreateProject()
    - QuestionQueue reads unanswered questions from Zustand, shows badge count
    - page.tsx no longer shows "Dashboard coming in Phase 4" placeholder
    - All sidebar components are 'use client'
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>Dashboard shell with sidebar, project list, question queue, and new project dialog</done>
</task>

<task type="auto">
  <name>Task 2: Project detail page + run list + run page shell</name>
  <files>apps/web/app/(dashboard)/projects/[projectId]/page.tsx, apps/web/app/(dashboard)/projects/[projectId]/runs/[runId]/page.tsx</files>
  <read_first>
    - apps/web/lib/hooks/use-projects.ts (useRuns hook -- from Plan 04)
    - apps/web/lib/hooks/use-run-actions.ts (useStartRun -- from Plan 04)
    - apps/web/lib/hooks/use-sse.ts (useRunStream -- from Plan 04)
    - apps/web/lib/stores/ui-store.ts (setActiveProject, setActiveRun -- from Plan 04)
    - .planning/phases/04-research-sprint-workflow/04-CONTEXT.md (D-20, D-21)
  </read_first>
  <action>
**1. Project Detail: apps/web/app/(dashboard)/projects/[projectId]/page.tsx** (per D-20)

'use client' component:
- Read `projectId` from params (await params pattern for Next.js 15)
- Call `useRuns(projectId)` to get runs for this project
- Set `useUIStore.getState().setActiveProject(projectId)` in useEffect
- Show project header with name (could fetch from useProjects or pass via a separate hook)
- Show run list as cards: each card shows run status (Badge), prompt truncated, created date
- Clicking a run navigates to `/projects/${projectId}/runs/${runId}`
- "New Sprint" section at top: Textarea for prompt + "Start Sprint" button
- Start Sprint calls `useStartRun()` with `{ projectId, prompt }`, on success navigates to new run page
- Status badges colored: pending=gray, planned=yellow, approved=blue, executing=green animated pulse, completed=green, cancelled=red

**2. Run Page: apps/web/app/(dashboard)/projects/[projectId]/runs/[runId]/page.tsx** (per D-20, D-21)

'use client' component:
- Read `projectId` and `runId` from params
- Call `useRunStream(runId)` to connect SSE
- Call `useUIStore.getState().setActiveRun(runId)` in useEffect
- This page is the transcript container. For now, render a basic layout:
  - Header: Run status badge + prompt text
  - Transcript area: will be filled by Plan 06 components
  - For now, render a simple message list from `useRunStore((s) => s.messages)` -- each message shows agentId, type, content.text, timestamp
  - Composer at bottom (will be built properly in Plan 06, but create a basic input + send placeholder)
- The transcript components from Plan 06 will replace the basic list

This page must:
- Connect to SSE on mount via useRunStream(runId)
- Show events as they arrive in real-time
- Show run status from useRunStore
  </action>
  <verify>
    <automated>cd /Users/lucastraber/Projects/beagle-console && pnpm tsc --noEmit -p apps/web/tsconfig.json 2>&1 | tail -5 && test -f "apps/web/app/(dashboard)/projects/[projectId]/page.tsx" && echo "project-page EXISTS" && test -f "apps/web/app/(dashboard)/projects/[projectId]/runs/[runId]/page.tsx" && echo "run-page EXISTS"</automated>
  </verify>
  <acceptance_criteria>
    - Project page shows run list with status badges
    - Project page has "New Sprint" form with textarea + start button
    - Start button calls useStartRun mutation and navigates to new run
    - Run page connects to SSE via useRunStream(runId)
    - Run page renders events from useRunStore in real-time
    - Run page shows run status badge
    - Both pages are 'use client' components
    - Both pages set active project/run in UI store
    - TypeScript compiles clean
  </acceptance_criteria>
  <done>Project detail page with run management and run page with live SSE transcript</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client-side routing | URL params (projectId, runId) are untrusted; API routes validate ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Information Disclosure | URL-based projectId/runId | mitigate | API routes verify resource belongs to tenant; client just renders what API returns |
</threat_model>

<verification>
- All page files exist at correct Next.js App Router paths
- Dashboard layout has sidebar with project list and question queue
- Project page has run list + new sprint form
- Run page connects to SSE and shows live events
</verification>

<success_criteria>
- User can navigate: dashboard -> project -> run
- User can create a new project via sidebar dialog
- User can start a new sprint from project page
- Run page shows real-time events via SSE
- Question queue in sidebar shows unanswered count
</success_criteria>

<output>
After completion, create `.planning/phases/04-research-sprint-workflow/04-05-SUMMARY.md`
</output>
