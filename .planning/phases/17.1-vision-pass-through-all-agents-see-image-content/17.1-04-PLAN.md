---
phase: 17.1-vision-pass-through-all-agents-see-image-content
plan: 04
type: execute
wave: 4
depends_on: ["17.1-01", "17.1-02", "17.1-03", "17.1-05", "17.1-06", "17.1-07"]
files_modified:
  - /opt/beagle-console/.env (PROD — env injection, NOT in repo)
  - /opt/beagle-console/docker-compose.yml (PROD — env block injection, NOT in repo)
autonomous: false
requirements: [UAT-17-1-01, UAT-17-1-02, UAT-17-1-03, UAT-17-1-04, UAT-17-1-05, UAT-17-1-06, UAT-17-01, UAT-17-03]
tags: [deploy, migration, uat, blocking, anthropic-key, prod-env]

user_setup:
  - service: anthropic
    why: "Vision-API description for image attachments — direct Anthropic SDK call (LiteLLM is down)"
    env_vars:
      - name: ANTHROPIC_API_KEY
        source: "console.anthropic.com → API Keys → Create Key (or reuse Lucas's existing personal key if no separate console-web key exists)"
    dashboard_config: []

must_haves:
  truths:
    - "Pre-flight typecheck + vitest pass before pushing to main"
    - "ANTHROPIC_API_KEY is set in /opt/beagle-console/.env on BeagleHQ AND injected into the console-web service's environment block in /opt/beagle-console/docker-compose.yml"
    - "migrate-17-1.ts runs successfully against the live Postgres BEFORE container rebuild — every tenant schema has artifacts.description column"
    - "console-web container is force-recreated with the new image"
    - "console-agent-hub container is force-recreated with the new image (because Plan 17.1-03 modified hub Zod and CLI bridge)"
    - "Smoke checks pass: web /api/health returns 200; hub /health returns 200; console.beaglemind.ai loads"
    - "UAT-17-1-01 verified: uploading an image to a fresh run produces a non-null description in artifacts.description AND the description appears in agent-hub logs as part of the prepended block AND all three agents (Mo, Jarvis, Herman) reference content (visual or descriptive)"
    - "UAT-17-1-02 verified: Mo and Jarvis reference specific visual details that go beyond what's in the description (proves bytes reached them) — OR explicitly waived if Plan 17.1-03 outcome D"
    - "UAT-17-1-03 verified: Herman references the description content in its reply"
    - "UAT-17-1-04 verified: uploading a .md file via paperclip on Windows succeeds; chip flips to ready; agents receive the markdown content"
    - "UAT-17-1-05 verified: user message bubble shows attachment chips (NOT extracted text); clicking chip opens preview/download; image attachments show inline thumbnail with click-to-fullsize"
    - "UAT-17-1-06 verified: follow-up message sent to a completed run triggers a fresh round-table where agents reference prior conversation content; status flips executing → completed correctly"
    - "UAT-17-01 re-tested (was Pending after Lucas's UAT failure on MD upload): now closed via 17.1-05"
    - "UAT-17-03 re-tested (was Pending blocked on 17.1): closed via 17.1-02 + 17.1-06 (image desc reaches agents AND extracted text reaches agents but stays out of transcript bubble)"
  artifacts:
    - path: "/opt/beagle-console/.env"
      provides: "ANTHROPIC_API_KEY env var for the console-web container"
      contains: "ANTHROPIC_API_KEY="
    - path: "/opt/beagle-console/docker-compose.yml"
      provides: "console-web service environment includes ANTHROPIC_API_KEY interpolation"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "/opt/beagle-console/.env"
      to: "console-web container env"
      via: "docker-compose env_file or environment ANTHROPIC_API_KEY interpolation"
      pattern: "ANTHROPIC_API_KEY"
    - from: "extractImageDescription"
      to: "process.env.ANTHROPIC_API_KEY"
      via: "Anthropic SDK client init"
      pattern: "ANTHROPIC_API_KEY"
---

<objective>
Ship Phase 17.1 to production. Pre-flight, push, set ANTHROPIC_API_KEY, run [BLOCKING] migrate-17-1.ts BEFORE container rebuild, force-recreate console-web AND console-agent-hub, smoke check, and human-verify all three UAT requirements with a fresh run carrying one image (and ideally one PDF).

Purpose: Plans 17.1-01..03 are code-complete but the description column doesn't exist on the live database, the Anthropic API key isn't set, and the agent-hub container hasn't been rebuilt with the new Zod schema and CLI bridge changes. This plan operationalizes everything.

Output: A live, verified deploy with explicit human sign-off on UAT-17-1-01..03.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-CONTEXT.md
@.planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-PATTERNS.md
@.planning/phases/17-attachments/17-PATTERNS.md
@.planning/phases/17.1-vision-pass-through-all-agents-see-image-content/openclaw-flag-verification.md

<interfaces>
Production deploy targets:
- Build dir on prod: `/tmp/beagle-build` (use `sudo git -C /tmp/beagle-build` due to .git ownership quirks)
- Compose file on prod: `/opt/beagle-console/docker-compose.yml` (drifts from repo's `docker/docker-compose.yml` — Phase 17 patched MINIO config on prod only)
- Postgres container: `sonic-hq-postgres-1`. DB user: `beaglehq`. DB name: `beagle_console`.
- Hanseatic tenant id: `eb61fa6a-1392-49c2-8209-ae8fa3612779`. Schema: `tenant_eb61fa6a_1392_49c2_8209_ae8fa3612779`.
- Web container name: `beagle-console-console-web-1`. Force-recreate via `cd /opt/beagle-console && sudo docker compose up -d --force-recreate console-web`.
- Hub container name: `beagle-console-console-agent-hub-1` (verify with `sudo docker ps | grep agent-hub`).
- BeagleHQ SSH alias: confirm via `git log` of prior Phase 17 deploy commit, OR use direct host (46.224.167.166).

Migration command (run from inside /tmp/beagle-build on the prod box):
```
pnpm --filter @beagle-console/db exec tsx src/scripts/migrate-17-1.ts
```
Reads DATABASE_URL from prod env.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight (typecheck + vitest + push)</name>

  <read_first>
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-01-PLAN.md (verify GREEN commits landed)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-02-PLAN.md (verify GREEN commits landed)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-03-PLAN.md (verify GREEN commits landed)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/openclaw-flag-verification.md (record UAT-17-1-02 expected outcome from 17.1-03 Task 1)
  </read_first>

  <files>/tmp/17.1-deploy-sha.txt (capture file, not source)</files>

  <action>
1. Verify all expected commits exist (run from repo root):
   ```
   git log --oneline -20 | grep -E "17\.1-0[1-3]"
   ```
   Expected:
   - `test(17.1-01): add failing tests for extractImageDescription (RED)`
   - `feat(17.1-01): add extractImageDescription + Anthropic SDK + description column + migrate-17-1 (GREEN)`
   - `feat(17.1-01): wire extractImageDescription into POST /attachments in parallel with extractAttachment`
   - `test(17.1-02): rewrite + add buildAttachmentBlock tests for description branch (RED)`
   - `feat(17.1-02): replace V1 image placeholder with vision-API description in attachment block (GREEN)`
   - `feat(17.1-02): project artifacts.description in messages route to feed buildAttachmentBlock`
   - `docs(17.1-03): verify OpenClaw CLI image flag for Track 2 implementation`
   - `feat(17.1-03): add visionCapable flag + MinIO base64 fetch + imageAttachments in startRun`
   - `feat(17.1-03): hub Zod widen + visionCapable gate + CLI bridge image pass-through`

   If any commit is missing, do NOT proceed — go fix the upstream plan.

2. Run pre-flight typecheck across all touched packages:
   ```
   corepack pnpm -F web exec tsc --noEmit
   corepack pnpm --filter @beagle-console/agent-hub exec tsc --noEmit
   corepack pnpm --filter @beagle-console/db exec tsc --noEmit
   ```
   All three must exit 0.

3. Run pre-flight vitest on apps/web:
   ```
   corepack pnpm -F web exec vitest run
   ```
   Full suite must be green. Expected case count ≥200.

4. Push to main:
   ```
   git push origin main
   ```
   The repo's CI/CD pipeline (per Phase 1's roadmap entry) builds container images. If the standard path is direct prod-side build (Phase 17's pattern: `sudo git -C /tmp/beagle-build pull`), capture the SHA and use it in Task 4.

5. Capture the deploy SHA:
   ```
   git rev-parse HEAD > /tmp/17.1-deploy-sha.txt
   cat /tmp/17.1-deploy-sha.txt
   ```
  </action>

  <verify>
    <automated>corepack pnpm -F web exec tsc --noEmit &amp;&amp; corepack pnpm --filter @beagle-console/agent-hub exec tsc --noEmit &amp;&amp; corepack pnpm --filter @beagle-console/db exec tsc --noEmit &amp;&amp; corepack pnpm -F web exec vitest run</automated>
  </verify>

  <acceptance_criteria>
    - All 9 expected commits from Plans 17.1-01..03 are visible in `git log --oneline`
    - `tsc --noEmit` exits 0 in apps/web, apps/agent-hub, packages/db
    - `vitest run` exits 0 in apps/web with ≥200 cases
    - `git push origin main` succeeds (or `git status` shows no diffs to push if a previous run already pushed)
    - `/tmp/17.1-deploy-sha.txt` contains the SHA being deployed
  </acceptance_criteria>

  <done>Local pre-flight clean; all 17.1-* commits on main; deploy SHA captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING — HUMAN] Set ANTHROPIC_API_KEY in /opt/beagle-console/.env on prod</name>

  <read_first>
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-CONTEXT.md (lines 39-47 — Anthropic SDK direct, env var requirement)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-PATTERNS.md (correction #2 at lines 11-13 — ANTHROPIC_API_KEY is NOT set anywhere in existing infra)
  </read_first>

  <files>/opt/beagle-console/.env, /opt/beagle-console/docker-compose.yml (PROD only — not in repo)</files>

  <what-built>
Plan 17.1-01's `extractImageDescription` reads `process.env.ANTHROPIC_API_KEY` at module load. Without this env var, every image upload produces description=NULL and agents fall back to `(image attached — description unavailable)` instead of the real description. Lucas must provide the key value (it's a personal API credential — Claude cannot generate it).
  </what-built>

  <how-to-verify>
1. Confirm or generate the API key (Lucas's task):
   - If a `console-web` Anthropic key already exists in 1Password / Lucas's vault, use that value.
   - If not, go to https://console.anthropic.com → API Keys → "Create Key" → name it `console-web-prod` → copy the value (only shown once).

2. SSH to BeagleHQ and inject the env var into `/opt/beagle-console/.env`:
   ```
   ssh root@beaglehq
   cd /opt/beagle-console
   sudo cp .env .env.bak.17-1.$(date +%Y%m%d-%H%M%S)
   echo 'ANTHROPIC_API_KEY=sk-ant-XXXX...' | sudo tee -a .env > /dev/null
   sudo grep ANTHROPIC_API_KEY .env
   ```
   Replace `sk-ant-XXXX...` with the actual value. The key MUST be on its own line, no spaces, no quotes.

3. Inject into docker-compose.yml so the console-web service receives the env var at container start:
   ```
   sudo cat /opt/beagle-console/docker-compose.yml | grep -A30 "console-web:" | grep -E "(env_file|environment:)"
   ```
   - If `env_file: - .env` is already present under console-web, no further action needed (auto-injected).
   - If the compose uses an explicit `environment:` block (most likely per Phase 17's pattern), add the entry. Edit via `sudo nano /opt/beagle-console/docker-compose.yml` and add the line `      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}` in the console-web service's environment block.

4. Verify the value will reach the container BEFORE rebuilding:
   ```
   cd /opt/beagle-console
   sudo docker compose config | grep -i anthropic
   ```
   Expected output: `ANTHROPIC_API_KEY: sk-ant-XXXX...` (decoded by docker-compose). If empty or missing, the env interpolation is wrong — fix before Task 4.

5. Reply with one of:
   - `injected` — env var is in .env AND in docker-compose.yml; `docker compose config` shows the decoded value
   - `skipped` — env var omitted intentionally (descriptions will be NULL until next deploy)
   - `blocked` — describe the issue (e.g. no key available, .env locked, etc.)
  </how-to-verify>

  <acceptance_criteria>
    - `/opt/beagle-console/.env` contains a single line starting with `ANTHROPIC_API_KEY=` (no quotes, value is non-empty)
    - `/opt/beagle-console/docker-compose.yml` console-web service env block references `ANTHROPIC_API_KEY` (either via `env_file: .env` or via explicit `environment:` interpolation)
    - `sudo docker compose config | grep -i anthropic` on prod shows the decoded key value (not empty, not the literal `${ANTHROPIC_API_KEY}` string)
    - Backup file exists at `/opt/beagle-console/.env.bak.17-1.<timestamp>`
    - Human signal received: `injected`, `skipped`, or `blocked`
  </acceptance_criteria>


  <action>See <how-to-verify> below — this is a checkpoint:human-action task. Lucas must SSH to BeagleHQ and inject ANTHROPIC_API_KEY into /opt/beagle-console/.env and the docker-compose service env block per the steps in <how-to-verify>. Claude cannot perform this task because it requires Lucas's personal Anthropic API credential.</action>

  <verify>
    <automated>ssh root@beaglehq "cd /opt/beagle-console &amp;&amp; sudo docker compose config" 2&gt;&amp;1 | grep -i anthropic | grep -v -E "\\$\\{ANTHROPIC_API_KEY\\}"</automated>
  </verify>

  <done>ANTHROPIC_API_KEY value visible (decoded) in docker compose config output on prod; backup .env file exists; Lucas has replied with `injected`, `skipped`, or `blocked`.</done>

  <resume-signal>Reply with `injected`, `skipped`, or `blocked` (with explanation). If skipped, the deploy proceeds but UAT-17-1-01 will likely fail.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Run migrate-17-1.ts against live Postgres BEFORE container rebuild</name>

  <read_first>
    - packages/db/src/scripts/migrate-17-1.ts (verify file exists and reads from `shared.organizations`)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-CONTEXT.md (cross-cutting: "[BLOCKING] schema-push ordering: migrate-17-1.ts runs BEFORE container rebuild")
  </read_first>

  <files>live Postgres tenant_*.artifacts (DDL only; no source files modified)</files>

  <action>
1. SSH to BeagleHQ and pull the latest source to `/tmp/beagle-build`:
   ```
   ssh root@beaglehq
   cd /tmp/beagle-build
   sudo git fetch origin main
   sudo git reset --hard origin/main
   sudo git rev-parse HEAD
   ```
   Confirm the SHA matches `/tmp/17.1-deploy-sha.txt` from local Task 1.

2. Install workspace deps (the migrate-17-1 script imports postgres + drizzle from packages/db):
   ```
   cd /tmp/beagle-build
   sudo corepack pnpm install --frozen-lockfile
   ```

3. Run migrate-17-1 against the live database — reads DATABASE_URL from prod env:
   ```
   cd /tmp/beagle-build
   set -a; sudo source /opt/beagle-console/.env; set +a
   sudo -E corepack pnpm --filter @beagle-console/db exec tsx src/scripts/migrate-17-1.ts 2>&1 | tee /tmp/17.1-migration-$(date +%s).log
   ```
   Expected output: one line per tenant (`Added tenant_<sanitized_id>.artifacts.description`), final `Phase 17.1 migration complete (<N> tenants).` line.

4. Verify the column landed in the Hanseatic schema (representative — confirms migration logic worked):
   ```
   sudo docker exec sonic-hq-postgres-1 psql -U beaglehq -d beagle_console -c \
     "\d tenant_eb61fa6a_1392_49c2_8209_ae8fa3612779.artifacts" \
     | grep description
   ```
   Expected output: `description | text |` (column type text, nullable).

5. Verify count of tenants migrated matches `shared.organizations`:
   ```
   sudo docker exec sonic-hq-postgres-1 psql -U beaglehq -d beagle_console -c \
     "SELECT count(*) FROM shared.organizations;"
   ```
   Should match the migration script's final log line.

6. Capture the migration log path for the SUMMARY:
   ```
   ls -la /tmp/17.1-migration-*.log
   ```

7. Do NOT proceed to Task 4 if the migration reported errors. The container rebuild requires the column to exist; otherwise inserts in `POST /api/runs/[id]/attachments` will throw at runtime (the Drizzle type accepts `description`, but the live DB won't).
  </action>

  <verify>
    <automated>ssh root@beaglehq "sudo docker exec sonic-hq-postgres-1 psql -U beaglehq -d beagle_console -t -c \"SELECT count(*) FROM information_schema.columns WHERE table_schema LIKE 'tenant_%' AND table_name = 'artifacts' AND column_name = 'description';\""</automated>
  </verify>

  <acceptance_criteria>
    - `/tmp/beagle-build` HEAD on prod matches `/tmp/17.1-deploy-sha.txt` from Task 1
    - migrate-17-1.ts ran and the log shows `Phase 17.1 migration complete (<N> tenants).` with N ≥ 1 (Hanseatic at minimum)
    - `\d tenant_eb61fa6a_1392_49c2_8209_ae8fa3612779.artifacts` lists `description | text` (column exists, nullable)
    - The count of tenant schemas with `artifacts.description` column equals the count of `shared.organizations` rows
    - No errors in the migration log (`grep -i 'failed\|error' /tmp/17.1-migration-*.log` returns no matches OR only matches inside the success message text)
  </acceptance_criteria>

  <done>Every tenant's artifacts table has the description column; the migration log is captured; the live DB is ready for the new container.</done>
</task>

<task type="auto">
  <name>Task 4: Force-recreate console-web AND console-agent-hub containers</name>

  <read_first>
    - planning_context "Production deploy environment notes" (compose file, container names, force-recreate command)
  </read_first>

  <files>PROD docker images: beagle-console-web, beagle-console-agent-hub (no source files)</files>

  <action>
1. Confirm Task 2 (env var injection) and Task 3 (migration) are both complete. If Task 2 was `skipped`, descriptions will be NULL — that's a UAT failure mode but doesn't block the deploy itself.

2. SSH to prod and pull source (probably already done in Task 3):
   ```
   ssh root@beaglehq
   cd /tmp/beagle-build
   sudo git fetch origin main
   sudo git reset --hard origin/main
   sudo git rev-parse HEAD
   ```

3. Build images for both services. The repo's docker setup builds both web and agent-hub in the same compose file. Force-recreate triggers a fresh build if the source changed:
   ```
   cd /opt/beagle-console
   sudo docker compose pull 2>/dev/null || true
   sudo docker compose build console-web console-agent-hub
   ```
   If `docker compose build` doesn't find the build context (prod compose pins pre-built images), build inside `/tmp/beagle-build` and tag:
   ```
   cd /tmp/beagle-build
   sudo docker build -f apps/web/Dockerfile -t beagle-console-web:latest .
   sudo docker build -f apps/agent-hub/Dockerfile -t beagle-console-agent-hub:latest .
   ```
   Confirm the docker-compose.yml in /opt/beagle-console references the right tag/image. Mirror Phase 16's deploy pattern from `.planning/phases/16-*/16-06-*-SUMMARY.md` if needed.

4. Force-recreate both containers:
   ```
   cd /opt/beagle-console
   sudo docker compose up -d --force-recreate console-web console-agent-hub
   ```

5. Verify containers are running:
   ```
   sudo docker ps --format '{{.Names}}\t{{.Status}}\t{{.Image}}' | grep -E "console-(web|agent-hub)"
   ```
   Expected: both containers show `Up <X> seconds` and the image SHA matches the new build.

6. Tail logs for 30 seconds to confirm clean startup:
   ```
   timeout 30 sudo docker logs -f beagle-console-console-web-1 2>&1 | head -100
   timeout 30 sudo docker logs -f beagle-console-console-agent-hub-1 2>&1 | head -100
   ```
   Look for:
   - Web: Next.js "Ready in <Xms>" or similar.
   - Web: NO `ANTHROPIC_API_KEY missing` warning IF Task 2 injected the key. (Warning fires at first vision call, not at startup, but a startup probe may also trigger it — it's a notice, not a failure.)
   - Hub: HTTP server listening on port 4100; no Zod parse errors.
  </action>

  <verify>
    <automated>ssh root@beaglehq "sudo docker ps --format '{{.Names}}\t{{.Status}}' | grep -E 'console-(web|agent-hub)' | grep -c 'Up'"</automated>
  </verify>

  <acceptance_criteria>
    - Both `beagle-console-console-web-1` and `beagle-console-console-agent-hub-1` show `Up` in `docker ps`
    - Both containers have an image SHA / created timestamp newer than the migration timestamp from Task 3
    - Web container logs do NOT show fatal startup errors (`grep -E "FATAL|ECONNREFUSED|EADDRINUSE"` returns no matches)
    - Hub container logs do NOT show fatal startup errors
    - `curl -sS https://console.beaglemind.ai/api/health` returns 200 OR the homepage loads (verify against Phase 1's INFR-01 setup)
    - `curl -sS http://localhost:4100/health` (run from prod) returns 200 (hub health endpoint per Phase 3's CONN-01)
  </acceptance_criteria>

  <done>Both containers running on the new SHA; logs clean; health endpoints responsive.</done>
</task>

<task type="auto">
  <name>Task 5: Smoke check the upload + description path with logs and DB query</name>

  <read_first>
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-CONTEXT.md (vision-API contract — model, prompt, max_tokens)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/openclaw-flag-verification.md (Plan 17.1-03 outcome shapes the grep pattern)
    - apps/web/lib/extract-attachment.ts (after Plan 17.1-01 — getClient + extractImageDescription)
  </read_first>

  <files>/tmp/17.1-smoke-web.log, /tmp/17.1-smoke-hub.log (capture files, not source)</files>

  <action>
1. Pre-position log tails BEFORE the human UAT in Task 6 begins:
   ```
   ssh root@beaglehq "sudo docker logs --tail 0 -f beagle-console-console-web-1" > /tmp/17.1-smoke-web.log 2>&1 &
   echo $! > /tmp/17.1-smoke-web.pid
   ssh root@beaglehq "sudo docker logs --tail 0 -f beagle-console-console-agent-hub-1" > /tmp/17.1-smoke-hub.log 2>&1 &
   echo $! > /tmp/17.1-smoke-hub.pid
   ```
   These run in the background and capture everything Task 6 produces. Don't kill them until after Task 6 finishes.

2. After Task 6's UAT message is sent (i.e., the human user has uploaded an image and clicked Send on a fresh run), query the DB to verify a description landed:
   ```
   ssh root@beaglehq "sudo docker exec sonic-hq-postgres-1 psql -U beaglehq -d beagle_console -c \"SELECT id, filename, mime_type, length(description) AS desc_len, left(description, 80) AS desc_head FROM tenant_eb61fa6a_1392_49c2_8209_ae8fa3612779.artifacts WHERE mime_type LIKE 'image/%' ORDER BY created_at DESC LIMIT 3;\""
   ```
   Expected: at least one row with `desc_len > 50` and a coherent `desc_head` (e.g. "A screenshot showing..." — not NULL, not the literal placeholder).

3. Verify the prompt-block flowed through the hub by grepping the agent-hub log for the canonical sentinels:
   ```
   grep -E "USER ATTACHMENTS|Description:|image attached — description unavailable" /tmp/17.1-smoke-hub.log | head -20
   ```
   Expected: at least one `--- USER ATTACHMENTS ---` line AND at least one `Description: ` line (proves Plan 17.1-02's block builder fired with the description from Plan 17.1-01).

4. For UAT-17-1-02 (vision-capable bytes pass-through), grep for the vision-flag invocation depending on Plan 17.1-03's outcome (record from openclaw-flag-verification.md):
   - Outcome A or B: `grep -cE "(--image|--attachment) '/tmp/console-vision-" /tmp/17.1-smoke-hub.log` should be ≥2 (Mo + Jarvis).
   - Outcome C: `grep -c "data:image/" /tmp/17.1-smoke-hub.log` should be ≥1.
   - Outcome D: `grep -c "imageAttachments received but OpenClaw" /tmp/17.1-smoke-hub.log` should be ≥1 per non-vision agent.

5. Stop the background log tails after Task 6 reports completion:
   ```
   kill $(cat /tmp/17.1-smoke-web.pid) $(cat /tmp/17.1-smoke-hub.pid) 2>/dev/null
   ```

6. Capture summary stats for the SUMMARY:
   ```
   wc -l /tmp/17.1-smoke-web.log /tmp/17.1-smoke-hub.log
   grep -c "extractImageDescription failed" /tmp/17.1-smoke-web.log || true
   ```
  </action>

  <verify>
    <automated>ssh root@beaglehq "sudo docker exec sonic-hq-postgres-1 psql -U beaglehq -d beagle_console -t -c \"SELECT count(*) FROM tenant_eb61fa6a_1392_49c2_8209_ae8fa3612779.artifacts WHERE mime_type LIKE 'image/%' AND description IS NOT NULL AND length(description) > 50;\""</automated>
  </verify>

  <acceptance_criteria>
    - At least one image artifact row in `tenant_eb61fa6a_..._artifacts` has `description IS NOT NULL` AND `length(description) > 50`
    - Agent-hub log contains `--- USER ATTACHMENTS ---` (≥1 match per agent that ran in the round-table — typically ≥3 for the 3-agent loop)
    - Agent-hub log contains `Description: ` (≥1 match — proves the new attachment-block format fired)
    - Agent-hub log does NOT contain the V1 placeholder `(image — included with this message)` (grep -c returns 0)
    - For Outcome A/B/C: vision-flag/inline-data-URI match count ≥ what's expected per the outcome
    - Web log does NOT show repeated `extractImageDescription failed` errors (1-2 transient retries are OK if Haiku occasionally rate-limits, but every image failing means the API key is wrong)
    - Smoke logs are saved at `/tmp/17.1-smoke-web.log` and `/tmp/17.1-smoke-hub.log` for the SUMMARY
  </acceptance_criteria>

  <done>The DB shows a real description for the uploaded image; hub log shows the new attachment block format; the V1 placeholder string is gone.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: [HUMAN UAT] Verify UAT-17-1-01..06 (vision + 3 defect fixes) with fresh run + image + .md + completed-run follow-up</name>

  <read_first>
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-CONTEXT.md (lines 130-134 — exact UAT requirement text)
    - .planning/phases/17.1-vision-pass-through-all-agents-see-image-content/openclaw-flag-verification.md (UAT-17-1-02 expected outcome — if D, mark UAT-17-1-02 as not-this-phase)
  </read_first>

  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md (status flips after sign-off)</files>

  <what-built>
End-to-end: image upload triggers Anthropic vision-API description (~1-2s); description persists to artifacts.description in the per-tenant schema; the messages route projects description through the buildAttachmentBlock helper into the prepended `--- USER ATTACHMENTS ---` block; the hub forwards the prompt + (for vision-capable agents) the base64 image bytes through the OpenClaw CLI bridge; Mo, Jarvis, Herman all see the description; Mo and Jarvis additionally see the actual pixels.
  </what-built>

  <how-to-verify>
1. Open https://console.beaglemind.ai (Lucas's normal session — Hanseatic tenant).

2. Create a fresh run (or open an existing project and start a new prompt). Make sure it's a NEW run — Phase 17 left descriptions=NULL on prior image artifacts.

3. Attach ONE clearly visual image to the composer via the paperclip button or drag-drop. Suggested fixture:
   - A screenshot from your existing notes / Bloomberg / Slack — anything where there's clear text or layout structure that an agent could meaningfully describe.
   - Or a stock photo with a recognizable subject (chart, dashboard, document).

4. Optionally also attach a PDF for mixed-attachment verification (not required; the smoke check in Task 5 already validates mixed flow if you want to test it once).

5. Type a prompt that explicitly asks the agents about the image. Suggestions:
   - "What do you each see in the image I attached? Does it tell us anything useful for our current work?"
   - "Look at the attached image and describe what stands out. Mo, focus on governance signals; Jarvis, on commercial-risk angles; Herman, on stress-test concerns."

6. Click Send.

7. Wait for the round-table to complete (Mo → Jarvis → Herman, each ~10-30s).

8. Verify each agent's reply:

   ### UAT-17-1-01 — every agent references image content
   - **Mo's reply:** must reference the image somehow (visual detail OR a description-content callback). PASS if Mo says anything image-specific that maps to your screenshot's content.
   - **Jarvis's reply:** same bar.
   - **Herman's reply:** same bar — even though Herman doesn't see pixels, Herman should reference the description (e.g. "looking at the dashboard you shared..." or "the screenshot shows..."). If Herman just says "I see you've attached an image but can't view it" → FAIL UAT-17-1-01.

   ### UAT-17-1-02 — vision-capable agents reference visual details beyond the description
   - Look at Mo and Jarvis's replies. Did either of them reference a specific visual detail that's clearly in the IMAGE but NOT in the prompt's `Description:` text? Examples that would pass:
     - Mo says "the amber accent on the wordmark" but the description didn't mention "amber"
     - Jarvis says "I can read 'Q3 Revenue: $4.2M' from the chart" — exact text from the image, not paraphrased in the description
     - Either agent identifies a UI element by color or position the description glossed over
   - If Mo and Jarvis only paraphrase what's already in the `Description:` line, the bytes likely didn't reach them — go check the agent-hub log from Task 5 for the vision-flag invocation.
   - If Plan 17.1-03 outcome was D (no OpenClaw image flag), this UAT is WAIVED for this phase — record that explicitly. UAT-17-1-02 reopens when OpenClaw vision support ships.

   ### UAT-17-1-03 — Herman references the description content
   - Herman should NOT see pixels (visionCapable=false). Confirm by reading Herman's reply: it should reference content that maps to the `Description: <text>` line in the prompt block, NOT to specific pixel-level details Mo/Jarvis quoted.
   - Cross-check: in the agent-hub log from Task 5, the openclaw call for Herman should NOT have the `--image` flag (or for outcome C, no `data:image/` data-URI). Only Mo and Jarvis get bytes.

9. **NEW: UAT-17-1-04 — MD upload on Windows (DEFECT-17-A fix).** In the same composer:
   - Click paperclip; in the file picker, select a `.md` file from your Documents/Notes folder (or create one with `notepad notes.md`).
   - Confirm chip appears, flips to `ready` (no `Unsupported file type` error). Send a message asking the agents what's in it. At least one agent should quote the markdown content.
   - Repeat with drag-drop instead of paperclip.

10. **NEW: UAT-17-1-05 — Transcript renders chips, not text dumps (DEFECT-17-B fix).** Look at the user message bubble for the message you just sent (with the PDF + image from steps 4-5):
    - The bubble should show ONLY your prompt text — NOT the extracted PDF text body, NOT the `--- USER ATTACHMENTS ---` block.
    - Below the prompt text, you should see attachment chips: PDF rendered as a card with filename + size + View/Download buttons; image rendered as an inline thumbnail (max ~240px tall) clickable to open full-size.
    - Click "View" on the PDF chip → existing preview panel opens with the PDF rendered. Click the image thumbnail → full image opens in a new tab.
    - Negative check: scroll back through the agent-hub log from Task 5 — the agents' prompts SHOULD still contain the `--- USER ATTACHMENTS ---` block with extracted text. The change is UX-only; agents still see everything.

11. **NEW: UAT-17-1-06 — Agents respond on completed runs (DEFECT-17-C fix).**
    - Wait for the current run to flip to `completed` (the run-status chip in the header should change from amber `executing` to grey `completed`).
    - Type a follow-up message in the composer like *"Mo, can you summarize what we just discussed?"* — Send.
    - Confirm:
      - Status chip flips back to `executing` immediately.
      - Mo (and the other agents in the round-table) respond.
      - Mo's response references content from the EARLIER messages in this run (proves PRIOR CONVERSATION block reached the agent).
      - After all agents finish, status chip flips back to `completed`.
    - Optionally repeat with attachments — confirm both attachments AND prior context flow correctly.
    - Negative check: if any agent fails (e.g. CLI bridge timeout), you should see a one-line bubble like `(mo failed to respond — see logs)` instead of total silence.

12. Reply to the prompt with one of:
   - `passed` — all six UAT items verified per the criteria above
   - `partial: 17-1-01=pass, 17-1-02=waived (D), 17-1-03=pass, 17-1-04=pass, 17-1-05=pass, 17-1-06=pass` — list any waivers
   - `failed: <reason>` — describe which UAT failed and what you observed (pasting agent replies + screenshots helps debugging)
  </how-to-verify>

  <acceptance_criteria>
    - UAT-17-1-01 confirmed: every agent's reply references image content (visual or descriptive)
    - UAT-17-1-02 confirmed (or explicitly waived per Plan 17.1-03 outcome D): Mo or Jarvis quoted a pixel-level detail not in the description
    - UAT-17-1-03 confirmed: Herman's reply references the description content (proves description-only path works for non-vision agents)
    - UAT-17-1-04 confirmed: .md file uploads via paperclip + drag-drop on Windows
    - UAT-17-1-05 confirmed: user message bubbles render attachment chips (NOT extracted text); chips link to preview/download
    - UAT-17-1-06 confirmed: follow-up message on completed run triggers fresh round-table; agents reference prior context; status flips correctly
    - Human signal received: `passed`, `partial: ...`, or `failed: ...`
  </acceptance_criteria>


  <action>See <how-to-verify> below — this is a checkpoint:human-verify task. Lucas must open https://console.beaglemind.ai, create a fresh run, attach an image, send a prompt asking the agents about it, and verify each agent (Mo, Jarvis, Herman) references image content per the UAT criteria in <how-to-verify>. Claude cannot perform this task because it requires human judgment on whether agent replies meaningfully engage with the image.</action>

  <verify>
    <automated>echo "manual verification only — see acceptance_criteria"</automated>
  </verify>

  <done>Lucas signed off with `passed`, `partial: ...` (with per-UAT verdicts), or `failed: ...` (with reason). UAT-17-1-01..03 status updated in REQUIREMENTS.md and ROADMAP.md.</done>

  <resume-signal>Reply with `passed`, `partial: ...`, or `failed: ...` (with detail). On `failed`, the planner reopens the relevant track plan as a gap-closure plan (`/gsd-plan-phase 17.1 --gaps`).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Lucas → /opt/beagle-console/.env | Manual SSH-side secret injection; backup file written before edit |
| /opt/beagle-console/.env → console-web container | docker-compose env interpolation; secret only in container env, not in image |
| migrate-17-1.ts → live Postgres | Idempotent ALTER COLUMN IF NOT EXISTS — safe to re-run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-1-04-01 | Information Disclosure | ANTHROPIC_API_KEY committed to repo by accident | mitigate | Task 2 sets the key directly on prod, never in repo. .gitignore already blocks .env. The compose file references the var by name, not value. Backup file (`.env.bak.17-1.<timestamp>`) is also outside the repo. |
| T-17-1-04-02 | DoS | migrate-17-1.ts hangs / Postgres lock | mitigate | `ALTER COLUMN ... ADD COLUMN IF NOT EXISTS` is a metadata-only operation; Postgres takes a brief AccessExclusiveLock per table but releases immediately for ADD COLUMN with no DEFAULT and nullable. Pre-deploy timing: Hanseatic only (1 tenant). At-most-1s per ALTER. Per-tenant try/catch in the migration script. |
| T-17-1-04-03 | Tampering | Forgotten container rebuild → runtime mismatch between schema and code | mitigate | Task 4 force-recreates BOTH containers; Task 5 grep-validates the new attachment-block format actually fires. If the agent-hub didn't rebuild, Plan 17.1-03's hub Zod schema isn't deployed and `imageAttachments` would be silently dropped (Zod's default-strip behavior — but the optional field would fail validation only if max(4) is violated). |
| T-17-1-04-04 | Repudiation | UAT failure with no diagnostic trail | mitigate | Task 5 captures full log tails; Task 6 asks for replies-as-evidence. SUMMARY documents the actual UAT verdict. |
</threat_model>

<verification>
- All commits land on main; CI/CD or direct prod pull-and-build path used.
- ANTHROPIC_API_KEY in .env + compose interpolation; `docker compose config` shows decoded value.
- migrate-17-1.ts ran successfully; column count matches tenant count.
- Both containers force-recreated; no fatal startup errors; health endpoints OK.
- Smoke log shows `--- USER ATTACHMENTS ---` + `Description:` lines; V1 placeholder string is gone.
- DB query confirms at least one image artifact has a real description.
- Human UAT signed off (or partial waiver documented per outcome D).
</verification>

<success_criteria>
- Phase 17.1 deployed.
- Description column exists in every tenant schema.
- ANTHROPIC_API_KEY set on prod; vision calls actually fire.
- Mo and Jarvis reference visual details from the image (UAT-17-1-02).
- Herman references description content (UAT-17-1-03).
- All three agents engage with image content per UAT-17-1-01.
- ROADMAP.md and REQUIREMENTS.md updated to mark UAT-17-1-01..03 complete (and UAT-17-03 unblocked, since 17.1 satisfies its blocking criterion).
</success_criteria>

<output>
After completion, create `.planning/phases/17.1-vision-pass-through-all-agents-see-image-content/17.1-04-SUMMARY.md` covering: commit list, env var injection method (env_file vs explicit env block), migration log path + tenant count, container rebuild SHA, smoke log paths, UAT verdict per requirement, threat-model status, deferred items (cost telemetry, async description, LiteLLM migration once Henrik's stack is back).
</output>
